# Native privacy-filter service dependencies.
#
# The opf package pulls the inference runtime (torch and friends) and is
# installed from its repository; it has no PyPI release. Weights are NOT
# included: `opf` downloads openai/privacy-filter (~3 GB BF16) to
# ~/.opf/privacy_filter on first model construction, or use OPF_CHECKPOINT
# to point at an existing checkpoint.
#
# fastapi/uvicorn versions below are the pair this service was written and
# contract-tested against (deployment/test_privacy_filter_api.py).
fastapi==0.141.1
uvicorn==0.53.0
opf @ git+https://github.com/openai/privacy-filter.git
