# Harness parity: server restart / crash recovery (docs/fidelity/HARNESS_PARITY.md).
#
# `review` shows nothing after a restart.
#
# C evidence: init_review_strings() blanks all 25 slots of review[] and is called
# from boot_world_files() on every boot (src/db.c:250-261, called at :293). The
# Go port used to serialize the buffer into data/world_state.json and replay it,
# which is why a rebooted server still answered `review` with pre-restart gossip.
#
# The actor gossips, reads the buffer back, quits (so both engines save through
# their own quit path) and restarts; the post-restart `review` must be the header
# and nothing else, and `look` proves the character really came back.

# The actor returns to the immortal start room (1204) before quitting so the
# login room is one both engines already agree on: C records no load room above
# LVL_IMMORT (src/act.other.c do_quit) and falls back to r_immort_start_room
# (src/interpreter.c:2195-2199). Quitting anywhere else would exercise
# DP-1310's still-open load-room path (docs/fidelity/depth/save.tsv).

[fixture]
empty-players
quiet-mobs

[setup:oracle]
Restartgoss
Y
oraclepass
oraclepass
N
M
H
W
K
Y
<ENTER>
1

[setup:port]
Restartgoss
y
oraclepass
oraclepass
N
M
H
W
K
Y
<ENTER>
1

[relogin:oracle]
Restartgoss
oraclepass
<ENTER>
1

[relogin:port]
Restartgoss
oraclepass
<ENTER>
1

[warmup]
goto 8162

[probe]
gossip the goblin army marches at dawn
review
goto 1204
quit
<RESTART>
goto 8162
review
look
