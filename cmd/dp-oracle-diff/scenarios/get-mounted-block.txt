# depth-case: get.mounted-block
# C get_from_room (act.item.c:297) rejects a mounted actor before any lookup:
# "You can't reach it from your mount." A floor object plus a rideable horse
# (both spawned in the 8162 actor room, mirroring movement-mounted) make the
# gate deterministic. `ride horse` is already proven identical by the movement
# depth pass; the diffed byte is the following `get bread`.

[fixture]
quiet-mobs
set-room-flag 8162 3 off
spawn-mob 8021 1 8162 80
spawn-obj 8010 1 8162 80

[setup:oracle]
Getmount
Y
oraclepass
oraclepass
N
M
H
W
K
Y
<ENTER>
1

[setup:port]
Getmount
y
oraclepass
oraclepass
Y
N
M
H
W
K
Y
<ENTER>
1

[probe]
ride horse
get bread
