# Direct dependencies for the Dark Pawns Python tooling/scripts.
# Ranges are bounded at the next major version for reproducibility; pin exact
# versions in requirements-lock.txt (regenerate with: pip freeze > requirements-lock.txt).
openai>=2.34.0,<3.0.0
anthropic>=0.100.0,<1.0.0
litellm>=1.83.0,<2.0.0
mem0ai>=2.0.0,<3.0.0
requests>=2.32.3,<3.0.0
websocket-client>=1.9.0,<2.0.0
redis>=8.0.1,<9.0.0
