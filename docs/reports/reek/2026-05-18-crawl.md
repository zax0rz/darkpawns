# Reek Crawl — 2026-05-18 (Monday)

**Deep dive:** pkg/combat/ (4,509 lines)

## Toolchain

- `go vet ./...` — clean
- `staticcheck ./...` — 1 new finding (2 suppressed via nolint)
- `golangci-lint ./...` — 22 errcheck (mostly tracked DP-102), 2 new QF findings

## Build

- `go build ./...` — passes
- `go test ./combat/...` — passes (cached)

## Commits since last crawl

2 commits (BRENDA, 2026-05-17):
- 9806cb4 fix: 4 HIGH spell fidelity bugs (DP-175, DP-176, DP-177, DP-178)
- f1ed5eb fix: 3 CRITICAL spell fidelity bugs (DP-172, DP-173, DP-174)

All previously reported CRITICAL/HIGH spell findings resolved.

## New Findings

### LOW

**LOW-030: fight_core.go:1101 — nested if/else chain on attackType should use switch**
- **File:** pkg/combat/fight_core.go:1101
- **What:** `if attackType == SKILL_BACKSTAB { ... } else if attackType == SKILL_CIRCLE { ... }` should use tagged switch
- **Why it matters:** Same QF1003 pattern as DP-9 (LOW-025, fight_core.go:872). Consistency issue.
- **Source:** golangci-lint (staticcheck QF1003)
- **Suggested fix:** Tagged switch on attackType

**LOW-031: dreaming/extract.go:107 — switch-with-if statements should use tagged switch**
- **File:** pkg/dreaming/extract.go:107
- **What:** `case low == "betrayal" || low == "backstab" || low == "attack_ally":` pattern in switch-on-true should use tagged switch on `low`
- **Why it matters:** Style only, no behavioral impact
- **Source:** golangci-lint (staticcheck QF1002)
- **Suggested fix:** `switch low { case "betrayal", "backstab", "attack_ally": ... }`

### Already Tracked (no new issues)

- 6 unused clamp functions (DP-171, LOW)
- 22 errcheck issues (DP-102, MED — resolved)
- equipment.go SA4004 (suppressed via nolint — intentional future multi-slot support)
- spec_procs3.go SA4000 (suppressed via nolint — intentional independent RNG rolls)

### Combat Deep Dive Notes

- fight_core.go has comprehensive nil-guard patterns on hook vars — good
- Combat engine correctly uses separate death path (DeathFunc callback) from fight_core.go hooks
- No data races detected in combat engine (snapshot-then-process pattern under RLock)
- `NowUnix` defaults to `return 0` with no game-layer override — death timestamps always 0 if fight_core.go TakeDamage path is used. **Not filing** since the combat engine's `handleDeath` uses `ce.DeathFunc` (wired), and fight_core.go hooks are mainly future wiring.

## Summary

0 critical, 0 high, 0 medium, 2 low
