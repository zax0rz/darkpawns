# Overnight Crawl — 2026-05-19 (Tuesday)

**Agent:** Reek
**Time:** 4:00 AM ET
**Scope:** Full codebase crawl + Tuesday fidelity deep dive (pkg/game/ vs handler.c, act.informative.c)

## Executive Summary

Clawpatch reviewed 2 features, found 7 issues (all in CLI tooling). Go vet clean. Staticcheck found 4 (2 nolint-commented, 1 backward compat, 1 real). 5 commits since yesterday (all BRENDA). Fidelity deep dive found 2 HIGH and 2 MEDIUM divergences from C source.

**Total: 16 findings — 4 HIGH, 4 MEDIUM, 8 LOW.**
**Linear issues: DP-200 through DP-211 (12 issues created).**

---

## Clawpatch Findings (7)

All in CLI tooling (profiling/profiler.go, benchmarks/websocket_benchmark.go). Not game engine. No C source equivalents.

### HIGH

**HIGH-001:** pprof auth bypass — 4 endpoints unauthenticated (profiler.go:578-591) → DP-200
- `/debug/pprof/cmdline`, `/profile`, `/symbol`, `/trace` lack auth wrapper
- Fix: wrap all 5 handlers with BasicAuth closure

**HIGH-002:** benchmark data race — duplicate client entries (websocket_benchmark.go:252-294) → DP-201
- Handler and setup loop both append connections. Double sends, double Close().
- Fix: remove append at line 286

### MEDIUM

**MED-001:** PerformanceMonitor.Stop() panics on double-close (profiler.go:500-502) → DP-206
- `close(pm.stopChan)` without sync.Once
- Fix: `pm.once.Do(func() { close(pm.stopChan) })`

**MED-002:** benchmark functions unreachable (benchmarks/websocket_benchmark.go) → DP-204
- File not named _test.go, go test never discovers benchmarks
- Fix: rename to websocket_benchmark_test.go

### LOW

**LOW-001:** pprof goroutine leak on SIGINT (profiler.go:599-714) → DP-211
**LOW-002:** stale closed-file references (profiler.go:249-340) → DP-209
**LOW-003:** unchecked type assertion (profiler.go:530-534) → DP-208

---

## Fidelity Findings (5)

Tuesday deep dive: `pkg/game/`, `pkg/game/systems/` vs `handler.c`, `act.informative.c`, `constants.c`

### HIGH

**HIGH-003:** wield weight check hardcoded at 50 (item_equipment.go:125) → DP-203
- C: `str_app[STRENGTH_APPLY_INDEX(ch)].wield_w` (str=10→115, str=18→255, str=25→1750)
- Go: `obj.GetWeight() > 50` regardless of strength
- str_app table already ported in pkg/combat/formulas.go but equip code doesn't use it
- Every character limited to weight ≤ 50 weapons

**HIGH-004:** mobHasFlag() always returns false (graph.go:350-352) → DP-202
- Called from graph.go:163 for sentinel mob tracking check
- C: `MOB_FLAGGED(to, MOB_SENTINEL)` prevents tracking
- Go: stub returns false, all sentinel mobs trackable
- MobInstance.HasMobFlag() exists but World.mobHasFlag bridge unimplemented

### MEDIUM

**MED-003:** scrounge creates object but never adds to inventory (skills2.go:84-91) → DP-205
- NewObjectInstance() creates food, returns Success, but item never enters inventory
- Player sees "You find X." but gets nothing

**MED-004:** bufferLogger unused — admin log capture broken (cmd/server/main.go:207-210) → DP-207
- Regression from deadlock fix (66e7e08)
- bufferLogger created then discarded with `_ = bufferLogger`
- Admin /logs endpoint returns empty

### LOW

**LOW-004:** behead always assumes slash weapon (skill_special.go:94) → DP-210
- Messaging only, not mechanics

---

## Toolchain Results

- **go vet:** clean
- **staticcheck:** 4 findings
  - equipment.go:235 — SA4004 loop unconditionally terminated (nolint, correct behavior)
  - save.go:333,337 — SA1019 deprecated a.Type (backward compat, known)
  - spec_procs3.go:903 — SA4000 identical expressions (nolint, intentional double RNG)
- **golangci-lint:** 24 issues (22 errcheck on defer Close/test code, 2 staticcheck duplicates)

---

## Commits Since Last Crawl (5)

1. `66e7e08` — BRENDA: remove slog.SetDefault deadlock (→ MED-004 regression)
2. `da4eeb9` — BRENDA: resolve DP-168, DP-171, DP-179, DP-180 (dead code, stack IDs, debug prints)
3. `c73242e` — BRENDA: resolve DP-185, DP-186, DP-187, DP-188 (affect bitmask, hellfire dice)
4. `fa13c6b` — BRENDA: Lua script deployment (28 scripts, 41 mobs)
5. `fad534d` — BRENDA: docs (session 47 notes)

---

## Silent Simplification Scan

Found 11 simplified/stub/placeholder comments in pkg/game/:

| File | Line | Comment | Severity |
|---|---|---|---|
| item_equipment.go | 125 | `// simplified str_app check` | HIGH-003 |
| comm_channel.go | 136 | `// Simplified: NPCs check obj list` | documented, LOW |
| comm_channel.go | 158 | `// Simplified: single target` | documented, LOW |
| graph.go | 352 | `// stub until mob flag access is implemented` | HIGH-004 |
| act.go | 176 | `// Simplified CAN_SEE_OBJ` | documented, LOW |
| skill_special.go | 94 | `// simplified — assume equipped weapons are slash-able` | LOW-004 |
| other_economy.go | 172 | `// Simplified: just use the item` | documented, LOW |
| skills2.go | 84 | `// placeholder: add item to inventory` | MED-003 |
| skills2.go | 336 | `// Simplified: single target` | documented, LOW |
| skills2.go | 383 | `// Simplified version: dig in current room` | documented, LOW |
| skill_stealth.go | 63 | `// Simplified: steal gold or an item` | documented, LOW |

---

*Report ends. Daeron triages.*
