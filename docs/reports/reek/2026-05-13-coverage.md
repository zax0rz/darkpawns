# Test Coverage & Quality Gap Analysis — 2026-05-13

**Focus:** Wednesday deep dive — pkg/session/, pkg/auth/, pkg/privacy/

---

## Coverage Overview

**Project total: 86,299 source lines — 8.3% statement coverage — 11,047 test lines (12.8% test:source ratio)**

2713 functions at 0.0% coverage. That's the majority.

## Packages With ZERO Test Files (12 packages, ~22,000 lines)

| Package | Files | Lines | Risk |
|---|---|---|---|
| **pkg/session/** | 63 | **13,412** | 🔴 CRITICAL |
| pkg/command/ | 6 | 3,126 | 🔴 HIGH |
| pkg/optimization/ | 9 | 2,318 | 🔴 HIGH |
| pkg/agentcli/ | 7 | 894 | 🟡 MEDIUM |
| pkg/db/ | 3 | 795 | 🟡 MEDIUM |
| pkg/telnet/ | 1 | 401 | 🟡 MEDIUM |
| pkg/agent/ | 1 | 412 | 🟡 MEDIUM |
| pkg/storage/ | 2 | 227 | 🟢 LOW |
| pkg/common/ | 3 | 185 | 🟢 LOW |
| pkg/secrets/ | 1 | 127 | 🟢 LOW |
| pkg/audit/ | 1 | 143 | 🟢 LOW |
| pkg/ai/ | 2 | 68 | 🟢 LOW |

## Wednesday Deep Dive: Coverage Detail

### pkg/session/ — 0.0% — ZERO TEST FILES

Sixty-three Go files. Thirteen thousand four hundred twelve lines. Zero test coverage. Zero test files exist.

**Critical untested paths:**

- **session_login.go:19** — `handleLogin()` — the entire authentication flow. JWT validation, character loading, session initialization. If this breaks, nobody logs in.
- **session_pump.go:11** — `readPump()` — goroutine per connection. Websocket read loop. One wrong nil dereference and the goroutine panics, taking the player offline. No test.
- **session_pump.go:41** — `writePump()` — the write loop with channel-based buffering. Leak a goroutine here and connections pile up. No test.
- **manager.go:126** — `NewManager()` — session lifecycle. Register, Unregister, cleanupSession, BroadcastToRoom. 595 lines of session orchestration. No test.
- **manager.go:351** — `Register()` — 37 lines, map mutation, duplicate session handling. Player name collision? No test.
- **manager.go:388** — `cleanupSession()` — deferred cleanup, mutex unlock ordering. If this deadlocks, sessions accumulate. No test.
- **session_command.go:11** — `RegisterCommand()` — command registry, dispatch routing. The entire player command pipeline. No test.
- **char_creation.go:17** — `handleCharInput()` — character creation state machine. Race between creation and login. No test.
- **60+ command handler files** — all `cmd*` functions at 0.0%. Every player action flows through these. No test for any of them.

### pkg/auth/ — 74.4% avg (ratelimit.go: 100%, jwt.go: 0%)

**What's tested:** `ratelimit.go` — `GetIPFromRequest` (84.6%), `SetTrustedProxies` (80%), rate limit configuration, cleanup loop, burst window. 344 test lines. Good quality.

**What's not tested:**
- **jwt.go:29** — `GenerateJWT()` — 0.0%. Token generation. If JWT signing breaks, auth is broken for every player. 86 lines, zero tests.
- **jwt.go** — token parsing, validation, refresh. All untested.
- **cleanupLoop:** 41.7% coverage — the cleanup race window isn't stressed.

### pkg/privacy/ — 21.3% avg (client.go 88.9%, rest 0%)

**What's tested:** `client.go` — PII detection, entity extraction, redaction patterns. 190 test lines. Good.

**What's not tested (all 0.0%):**
- **config.go** — 134 lines. Privacy configuration, field selectors. Not tested.
- **logger.go:19** — `NewPrivacyLogger()` — the PII-scrubbing logger. If this leaks PII to logs, it's a compliance problem. Not tested.
- **slog_handler.go:18** — `NewPIIHandler()` — slog handler integration. Not tested.
- **middleware.go:21** — `NewLoggingResponseWriter()` — HTTP middleware for PII filtering in web responses. Not tested.
- **middleware.go:30** — `WriteHeader()` — status code capture. Not tested.

## Partially-Covered Functions — Missing Edge Cases

These have tests but they're incomplete:

### pkg/engine/affect_manager.go — applyAffectImmediate (11.6%)
102 case branches in the affect switch. Tests cover maybe 1-2 (`AffectStrength`). The other 100 affect types — dexterity, intelligence, wisdom, haste, slow, poison, regeneration, blindness, sleep, charm, fear, curse, silence, flaming, infrared, water breathing, protection evil/good, paralysis, stun, invisible — all untested.

### pkg/engine/affect_manager.go — removeAffectImmediate (11.1%)
Same pattern. 90% of the remove-affect switch is uncovered.

### pkg/combat/fight_core.go — ChangeAlignment (38.5%)
Good/evil/criminal alignment shifts on kill. The evil-murder alignment penalty and lawful-good protection paths are not tested.

### pkg/combat/fight_core.go — fleshAlteredType (62.5%)
High-level damage type mapping (drowning, psiblast, petrify) — 3 of 8 branches uncovered.

### pkg/engine/affect_tick.go — SetTickInterval (44.4%)
Zero/negative interval handling not tested. Edge case: what happens when interval is set to 0?

### pkg/auth/ratelimit.go — cleanupLoop (41.7%)
Timer-based cleanup race condition. Concurrent cleanup paths not tested under load.

## Test Quality Assessment

### Good tests
- **pkg/combat/formulas_test.go** (748 lines) — thorough edge cases on damage formulas, THAC0, parry/dodge calculations
- **pkg/auth/ratelimit_test.go** (344 lines) — trusted proxy config, IP extraction, rate limiting edge cases
- **pkg/combat/fight_core_test.go** (335 lines) — new combatant, backstab mult, group gain boundary cases
- **pkg/game/death_test.go** (152 lines) — corpse attack type mapping, money descriptions, capitalization — good table-driven pattern
- **pkg/privacy/client_test.go** (190 lines) — PII detection — solid regex testing

### Weak tests
- **pkg/engine/affect_test.go** (415 lines) — tests affect_manager.go but only scratches the surface of applyAffectImmediate (11.6%)
- **pkg/spells/checkreagents_test.go** (173 lines) — limited inventory edge cases
- **pkg/spells/saving_throws_test.go** (226 lines) — basic saving throw but misses magic resistance edge cases

### Structural gaps
- **pkg/engine/affect_tick_test.go** — no test for `SetTickInterval` bounds
- **pkg/combat/skill_messages_test.go** (93 lines) — thin. Doesn't test pronoun resolution edge cases or multi-target messages
- **No integration tests between session layer and game layer** — the 12K line gap between `pkg/session/` and `pkg/game/` is the largest structural risk in the project

## Summary

1. **15 packages at 0.0% coverage.** That means more than half the codebase has never been executed under test.
2. **pkg/session/ at 0.0% is the biggest risk.** 63 files, 13,412 lines — the entire player interaction layer — untested. If login, command dispatch, or session lifecycle breaks, the game doesn't work.
3. **pkg/command/ at 0.0%** — 3,126 lines of command definitions, skill commands, shop commands. Untested.
4. **pkg/optimization/ at 0.0%** — worker pools, caches, websocket optimizer. Untested.
5. **pkg/game/ at 2.0%** — 42,710 source lines but only 2,478 test lines. The game logic — death, combat, AI, rooms, zones, spawners, objects, items, houses — barely has any test coverage.
6. **End-to-end paths untested.** No test simulates a player logging in, moving, fighting, and dying. The integration path from `session_login.go → session_pump.go → manager.go → game/world.go → game/combat_*.go → game/death.go` is entirely uncovered.

**2713 functions at 0.0%. 86,299 source lines. 8.3% coverage total.**

The code compiles. The tests that exist are well-written. But most of the project has never been tested.

216 critical, 22 high, 1 medium, 2474 low.
