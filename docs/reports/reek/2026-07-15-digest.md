# Weekly Research Digest — 2026-07-09 to 2026-07-15

## Reek Reports

| Metric | Count |
|---|---|
| New findings this week | 21 |
| All PENDING (untriaged) | 21 |
| HIGH | 9 |
| MED | 6 |
| LOW | 6 |

**Notable:** All 21 findings are PENDING. Triage backlog is accumulating — no triage run has processed these yet.

## Triage Outcomes (All Time)

| Status | Count |
|---|---|
| PENDING | 199 |
| REJECTED | 40 |
| FIXED | 16 |
| CONFIRMED | 0 |

## Codebase Changes

- **228 commits** in one week (Jul 8–15) — heaviest single-week sprint to date
- **14 PRs merged** (#334–#357)
- **Domain refactor landed** — score, who, consider, time/weather, channels/socials, directed speech, movement, equipment all aligned with C
- **CMWC PRNG ported** (PR #348) — C-compatible seeded random stream, unlocks deterministic combat/skill/spell testing
- **Character creation draw order** aligned with C (PR #349)
- **Skill system foundation** (PR #356) — C-faithful practice command, retired invented skill commands (DP-1116/1128/1129)
- **Combat round output + command messages** aligned with C (PRs #353, #355)
- **Command cleanup batch** (PR #357) — dead whisper/ask duplicates removed, title faithful to C
- **DP-1085 fixed** — hometown start rooms now honor C behavior

## Severity Distribution (This Week)

| Severity | Count | Notable |
|---|---|---|
| HIGH | 9 | Game loop panic recover, secrets exposure, path traversal, C source bugs |
| MED | 6 | Agent store persistence, affect determinism, unregistered commands |
| LOW | 6 | File mapping drift (stale docs), fantasy month names |

## Reek Behavior Shift

**New pattern (Jul 14):** Reek began auditing the original C source (`src/` files), not just Go. Six findings are C bugs: stack buffer overflow in `do_behead`, unchecked `fread` in `mail.c`, missing `House_save_control()`, `core_dump()` on block mismatch, OOB access in `spell_summon`, wrong byte order in `ident.c`.

This is significant for the paper. Reek is no longer just a Go code reviewer — it's now cross-referencing the C source against the Go port. The C oracle pipeline (PR #243) may have unlocked this by making the C source more accessible.

## Hot Zones

| Package/Area | Findings | Theme |
|---|---|---|
| `src/` (C source) | 6 | Original C bugs — overflow, OOB, missing saves |
| `pkg/engine/` | 3 | Game loop resilience, affect determinism |
| `pkg/session/` | 2 | Unregistered commands (do_detect, do_mold) |
| `audit_reports/` | 4 | Stale file mapping documentation |
| `pkg/secrets/`, `pkg/audit/` | 2 | Security — key exposure, path traversal |

## Key Observations

1. **The domain refactor is the story this week.** 228 commits in 7 days, 14 PRs, all aimed at aligning Go behavior with C. The CMWC PRNG port is the linchpin — it makes deterministic testing possible for the first time.

2. **Reek's triage backlog is growing.** 199 PENDING findings. The automated triage pipeline (from Sprint G) should be processing these, but the Jul 8–15 batch hasn't been touched. Need to check if the revalidate cron is running.

3. **Reek auditing C source is a paper contribution.** Cross-language bug detection — finding bugs in the original C that the Go port may or may not have inherited. This is exactly the kind of "oracle-guided triage" the paper needs.

4. **Security findings are real.** DP-1087 (secrets exposed via env var) and DP-1088 (path traversal in audit logger) are legitimate HIGHs that affect production. These should be escalated.

5. **File mapping is stale.** The domain refactor moved files around. 4 audit report entries point to files that no longer exist. Documentation debt accumulating alongside the code wins.

## Paper-Relevant Notes

- **CMWC PRNG port** enables deterministic differential testing across C→Go — the oracle harness now has a reproducible random stream. This is a methodological contribution: "seed both implementations identically and compare transcripts."
- **Reek's C source audit** (Jul 14) demonstrates cross-language bug detection without formal verification. Static analysis of C source through the lens of a Go port reviewer. Novel framing for AIIDE.
- **228 commits in a week** by a human + AI pipeline — measures the velocity of AI-assisted port fidelity work. Worth quantifying for the paper's "what the agent preserved" section.
