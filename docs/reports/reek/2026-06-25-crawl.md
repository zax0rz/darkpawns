# Reek Crawl — 2026-06-25

**Mode:** deep (deterministic pipeline)
**Head:** `6e1ac406` (same as last crawl — no new code changes)
**Toolchain:** all clean
**Coverage:** unchanged from June 17 baseline

## Pass Results

| Tool | Status | Issues |
|------|--------|--------|
| go_vet | ✅ | 0 |
| golangci-lint | ✅ | 0 |
| gofumpt | ✅ | 0 |
| go test | ✅ | all passed |
| go test -race | ✅ | 0 races |
| govulncheck | ✅ | 0 vulns |
| gosec | ⚠️ | 32 taint candidates — all FP under reachability |

## Gosec Triage

- **G404 (15x):** `math/rand` in game mechanics (combat rolls, AI, skills, spells) — not a security concern. Game RNG doesn't need crypto/rand.
- **G703 (10x):** Path traversal via `adminUIDir` from env var `ADMIN_UI_DIR` — attacker cannot control the path. `/admin/assets/` uses `http.Dir` with Go 1.22+ security checks. False positives.
- **G704 (2x):** SSRF via `DISCORD_WEBHOOK_URL` env var — server-configured URL, not user-supplied. False positive.
- **G115 (5x):** Integer overflow conversions on bounded internal values (`memStats.LastGC` nanosecond timestamp, mail int32→uint32, player stats, equipment values). All bounded; no exploit path.

No security findings after attacker-minded verification.

## Commit-Diff Review

No new commits since last crawl (`6e1ac406` == `prev_crawl_sha`). No regressions to report.

The merged PRs in ancestry (affects unification, spell routing, berserk/kuji-kiri, bucket B commands, toggle dispatch, donate/junk, telnet listener rewrite, golden tests) were reviewed in prior crawl. No dangling references to removed `affect_manager.go`. New code carries C cross-references.

## Coverage (unchanged)

Critical paths still dangerously low, already tracked as DP-606 through DP-610:
- `pkg/command` @ 3.2%
- `pkg/db` @ 17.8%
- `pkg/game` @ 14.7%
- `pkg/telnet` @ 19.8%
- `pkg/session` @ 22.1%
- `pkg/spells` @ 14.1%

**Summary:** 0 new findings. Clean run.
