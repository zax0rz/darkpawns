<@&1501658664623411212>

**MED-032: SpellChangeDensity reuses AffWaterwalk** → DP-82
pkg/spells/affect_spells.go:137 — SpellChangeDensity creates an affect with `engine.AffectWaterwalk` instead of a dedicated affect type.
both waterwalk and change density have the same runtime effect (waterwalking). change density is supposed to be something different. player casts "change density" and starts waterwalking. that's wrong.
source: analysis — affect type collision
suggested: add `AffectChangeDensity` constant to pkg/engine/affect.go and use it at affect_spells.go:137

**MED-033: luaSteal uses vnum=0 as sentinel for "any item"** → DP-83
pkg/scripting/engine.go (luaSteal function) — calls world.RemoveItemFromChar(name, 0) where 0 means "any item."
the interface doesn't document this sentinel value. if a mob legitimately has an item with vnum 0 (unlikely but possible), the wrong item gets stolen. also, RandomItemFromChar would be clearer.
source: analysis — undocumented sentinel
suggested: rename or add comment documenting 0 = "any item" semantics; add RandomItemFromChar interface method

**MED-034: SpellChangeDensity registered with wrong min position** → DP-84
pkg/spells/affect_spells.go init() — SpellChangeDensity is registered with MinPosition=PosStanding.
in the C source, change density is castable at any position (including resting/sitting). the current Go code requires standing. minor gameplay restriction.
source: analysis — position constraint mismatch
suggested: set MinPosition to PosSleeping or confirm C behavior

**LOW-026: client.Close errors unchecked in dp-agent** → DP-85
cmd/dp-agent/main.go:79,303 — deferred client.Close() return value not checked.
if the client connection fails to close cleanly, the error is silently swallowed. standard deferred Close pattern — common but technically a resource leak.
source: golangci-lint (errcheck)
suggested: add `_ = client.Close()` or log the error

**MED-035: golangci-lint errcheck — 25 new unchecked errors** → DP-86
25 errcheck findings across 8 files: cmd/dp-agent/main.go, pkg/admin/handlers_test.go, pkg/admin/log_buffer.go, pkg/agentcli/*.go, pkg/dreaming/dream.go, pkg/game/help.go, pkg/parser/parser_test.go.
most are deferred Close() calls and test-helper json.Unmarshal calls. each one means a resource or error is silently discarded. 4 of them are in production code paths (help.go, log_buffer.go, dreaming/dream.go).
source: golangci-lint (errcheck)
suggested: check and log errors in production paths; _ = for deferred closes

**LOW-027: admin/router.go QF1007 — merge conditional assignment** → DP-87
pkg/admin/router.go:118 — `allowed := false` followed by conditional assignment can be merged into declaration.
style only, no behavioral impact.
source: golangci-lint (staticcheck)
suggested: `allowed := r.Method != http.MethodGet` or equivalent

**LOW-028: admin/log_buffer.go QF1012 — WriteString + Sprintf → Fprintf** → DP-88
pkg/admin/log_buffer.go:88 — `sb.WriteString(fmt.Sprintf(...))` should be `fmt.Fprintf(&sb, ...)`.
redundant allocation, no behavioral impact. same pattern as LOW-016 (dreaming/graph.go).
source: golangci-lint (staticcheck)
suggested: switch to fmt.Fprintf

**LOW-029: admin/handlers_test.go QF1003 — nested if vs switch** → DP-89
pkg/admin/handlers_test.go:1524 — `if s == http.StatusOK` should be tagged switch.
test code, style only.
source: golangci-lint (staticcheck)
suggested: use tagged switch

---

**Friday deep dive: pkg/spells/, pkg/scripting/, pkg/secrets/ (9K lines)**

walked every .go file in spells (17 files), scripting (3 files), secrets (1 file).
spell system is in good shape — all spell info tables registered, MagDamage/MagAffects/MagPoints implemented for all routine types. 38 manual cast functions through ExecuteManualSpell. saving throw table ported verbatim from C for all 12 classes.
scripting engine has 70+ Lua API functions registered. sandboxing is thorough (dofile removed and re-registered, os.* removed, io/debug/package removed). path traversal protection in dofile. LState recreation on panic. transitItems with TTL-based cleanup.
secrets manager uses AES-256-GCM with ENCRYPTION_KEY from env. standard pattern.

gaps noted: SpellChangeDensity affect type collision (MED-032), Lua spell() function duplicates MagDamage formulas in luaSpace — the Go-side spell system handles damage through MagDamage which is the correct path. the Lua-side luaSpell function has its own damage formulas which may drift from the Go side. this is by design (scripts need to calculate their own damage for mob-vs-mob) but creates a maintenance burden.

1 critical (CRIT-011 still OPEN — ActiveAffects data race across 8+ files), 1 high (MED-032 new), 1 medium (MED-033 new), 4 low (LOW-026 through LOW-029 new), 1 medium (MED-035 new)

**Open tracker:** CRIT-011, MED-029, MED-030, MED-031, LOW-020, LOW-021, LOW-022, LOW-023, LOW-024, LOW-025
