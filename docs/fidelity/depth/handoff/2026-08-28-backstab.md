# Depth-fidelity handoff — 2026-08-28 — `do_backstab`

## Queue position

- The special-procedure inventory and the single `objmagic.sleep-entry-gates`
  attempt are complete.
- The interpreter-table sweep has completed `ambush`, `appraise`, `auto`, and
  now `backstab` in table order. The next family must be selected by a fresh
  main checkout, pull, frontier report, and C-table/manifest comparison.

## C path verified

- `src/interpreter.c:343` registers `backstab` at `POS_FIGHTING` with
  `do_backstab`.
- `src/act.offensive.c:165-231` uses `one_argument`, checks skill before target
  lookup, then applies target, self, wielded-weapon, piercing, mounted, and
  victim-fighting gates.
- An awake `MOB_AWARE` victim takes the three C `act` branches and immediate
  `hit(vict,ch)` path. Otherwise C draws `number(1,101)`, chooses the miss or
  `hit` path, improves only after the hit path, and applies the final
  `WAIT_STATE`.
- The skill damage path delegates to the existing combat and skill-message
  matrices; the backstab manifest records those boundaries rather than
  duplicating their shared coverage.

## RED finding and Go fix

- The C-first `backstab-depth` vehicle found that a missing target emitted
  `Backstab who?\r\n` in C while Go emitted the old generic
  `They don't seem to be here.\r\n`.
- The same audit confirmed that Go joined all command words while C
  `one_argument` skips fill words, lowercases the first non-fill token, and
  ignores the remainder. Go now uses the canonical C-faithful parser and the
  C missing-target line.
- The existing command test was updated from the old invented message to the
  C contract. No files under `src/` or `darkpawns-c-oracle/` were changed.

## Proof

- New vehicle: `cmd/dp-oracle-diff/scenarios/backstab-depth.txt` proves no-arg,
  missing-target, trailing-word, fill-word, first-token, and self-target
  command behavior. Seed 1 was run with `--show-oracle`; it has no normalized
  divergence.
- Existing `combat-skill-noskill` now contributes the annotated no-skill gate;
  seed 1 with `--show-oracle` is green.
- Existing `combat-backstab-opener` now contributes the annotated legal dagger
  opener. Seeds 1, 2, 3, 5, and 8 all have no normalized divergence.
- Focused backstab tests pass, including weapon/mount/fighting gates, aware
  retaliation, damage arithmetic, skill-message selection, and miss/hit RNG
  draw order.
- Manifest: `docs/fidelity/depth/backstab.tsv`, 16 rows — all proven.

## Gates

- `make fidelity-depth`: PASS — 706 total, 685 proven/delegated, 6 blocked,
  15 excluded; actionable 685/691 (99.1%).
- `go build ./...`: PASS.
- `go vet ./...`: PASS.
- `go test ./...`: PASS.
- `golangci-lint run ./...`: PASS — 0 issues.
- `gofumpt -l .`: PASS — clean.

## Carry-forward

- Branch: `glm/depth-backstab`.
- Base: main `53aaaa3e7` (auto merged); the unrelated untracked brief
  `docs/briefs/BRIEF-2026-08-28-economy-specproc-cluster.md` remains untouched.
- Open exactly one `glm/depth-backstab` family PR. Do not merge it unless every
  hosted check is green; if CI fails to fire, retry once with the prescribed
  workflow command and leave the PR open if it remains not-green.
