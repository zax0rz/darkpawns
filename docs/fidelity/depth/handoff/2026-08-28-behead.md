# Depth-fidelity handoff — 2026-08-28 — `do_behead`

## Queue position

- The special-procedure inventory and the single `objmagic.sleep-entry-gates`
  attempt are complete.
- The interpreter-table sweep has completed `ambush`, `appraise`, `auto`,
  `backstab`, `ban`, `do_not_here`, `bash`, `bearhug`, and now `behead` in
  table order. The next family must be selected from a fresh main checkout,
  pull, frontier report, and C-table/manifest comparison.

## C path verified

- `src/interpreter.c:350` registers `behead` at `POS_STANDING` with
  `do_behead` and no level gate.
- `src/new_cmds.c:215-365` parses with `one_argument`, resolves a visible
  character before the position/no-argument gates, rejects self and living
  targets, resolves a visible room object, rejects `headless`, and accepts
  only `ITEM_CONTAINER` objects with value[3] set.
- The success path selects slash messaging only when WEAR_WIELD value[3] is 3,
  creates proto 16 with rewritten head descriptions, applies `can_take_obj`,
  creates proto 17 with value[0]=0, value[3]=1 and the NPC corpse timer,
  transfers contents, and extracts the original object.
- `src/spec_procs3.c:198-220` contains an inactive brain-eater call site that
  invokes `do_behead` on `corpse`; no active special-procedure registration
  row was added because the registration is not reachable from the current
  dispatch tables.

## RED findings and Go fixes

- The original Go path required a `corpse` keyword and therefore rejected the
  C-valid ITEM_CONTAINER vnum 1210 (`fractal French tickler condom`). It now
  uses the canonical visible room-object resolver and the exact type/value
  predicate.
- The original path lacked the self-target branch, joined trailing argument
  words, used a non-canonical wielded-slot access, used the wrong player sex
  encoding for the room pronoun, and returned success messages with an extra
  newline. Those confirmed divergences are now aligned.
- The original synthetic head and replacement corpse had invented names and
  descriptions, were not reliably indexed in the room, did not preserve the
  C replacement values/timer/content transfer, and left the source object
  registered. The Go path now mirrors proto 16/17 creation, ordinary carry
  gating, central object movement, transfer, and extraction.
- No files under `src/` or `darkpawns-c-oracle/` were changed.

## Proof

- New live vehicle: `behead-object-depth`; it uses the C-valid non-corpse
  container, a named observer peer, trailing words, inventory, and look. It
  has no normalized divergence at seeds 1, 2, 3, 5, and 8; seed 1 was also
  run with `--show-oracle`.
- Focused tests cover self/living/missing/headless/non-container gates, slash
  messaging, exact head and corpse descriptions, replacement keywords and
  values, corpse timer, content transfer, source extraction, and one-argument
  parsing.
- Existing `circle-behead` now explicitly credits the no-argument branch.
- Manifest: `docs/fidelity/depth/behead.tsv`, 15 rows — all proven.

## Gates

- `make fidelity-depth`: PASS — 773 total, 752 proven/delegated, 6 blocked,
  15 excluded; actionable 752/758 (99.2%).
- `go build ./...`: PASS.
- `go vet ./...`: PASS.
- `go test ./...`: PASS.
- `golangci-lint run ./...`: PASS — 0 issues.
- `gofumpt -l .` via `make check-fmt`: PASS — clean.

## Carry-forward

- Branch: `glm/depth-behead`.
- Open exactly one family PR and merge only if every hosted check is green. If
  CI does not fire, retry once with the prescribed `gh workflow run` command;
  leave the PR open and advance if it remains not-green.
- Preserve the unrelated untracked brief
  `docs/briefs/BRIEF-2026-08-28-economy-specproc-cluster.md`.
