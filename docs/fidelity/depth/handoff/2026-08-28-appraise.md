# Depth-fidelity handoff — 2026-08-28 — `do_appraise`

## Queue position

- The special-procedure inventory and the single `objmagic.sleep-entry-gates`
  attempt are complete.
- The interpreter-table sweep has now completed `ambush` and `appraise` in
  order. The next genuinely un-manifested command family is `auto`
  (`src/interpreter.c:340`, `do_auto`); `ask` and `auction` are already covered
  by the existing communication manifests.

## C path verified

- `src/interpreter.c:1372-1379` — `half_chop` keeps only the first token in
  `arg` and leaves the remainder unused by `do_appraise`.
- `src/act.other.c:1784-1817` — `do_appraise` draws `number(1,101)` before
  every gate, searches only `ch->carrying` with `get_obj_in_list_vis`, uses the
  skill roll to choose bad versus good value, and calls `improve_skill` only on
  the success arm.
- `src/handler.c:1328-1352` — carried-object lookup uses ordinal parsing,
  keyword abbreviation, and visibility; equipment and room objects are not in
  this call path.
- `src/act.other.c:1704-1730` — `improve_skill` consumes the stat-gate draw,
  then one increment draw, and prints only on a +3 increment.

## RED findings and Go fix

- The baseline diverged when `appraise bread extra` was issued: C appraised
  `bread`, while Go searched for the whole string.
- The baseline diverged when a sword was wielded: C rejected it because the
  object was no longer carried, while Go's broad nearby-object helper
  appraised it.
- The low-cost success arm exposed Go's incorrect `number(-20,20)` range;
  C uses `number(0,20)` when cost is 20 or less. The old Go implementation
  also reimplemented improvement with an extra random draw for the message.
- Go now uses `halfChop`, `getObjInInvVis`, `ObjectInstance.GetCost`, the exact
  C low-cost lower bound, and shared `ImproveSkill`. The now-unused broad
  `findObjNear` helper was removed. No files under `src/` or
  `darkpawns-c-oracle/` were changed.

## Proof

- `appraise-depth`: no-arg, missing target, first-token/abbreviation, and
  successful carried-object arms; seed 1 `--show-oracle`, then seeds 1, 2, 3,
  5, and 8.
- `appraise-equipment`: wielded sword is refused by both implementations;
  seeds 1, 2, 3, 5, and 8.
- `appraise-cost`: carried bread success with cost 3 exercises the
  nonnegative low-cost range; seeds 1, 2, 3, 5, and 8.
- `appraise-failure`: skill-zero bad-value arm; seeds 1, 2, 3, 5, and 8.
- `TestDoAppraiseLowCostAndImproveDraws` proves the exact cost, skill
  increment, improvement message, and four-draw sequence.
- Manifest: `docs/fidelity/depth/appraise.tsv`, 7 rows, all proven.

## Gates

- `make fidelity-depth`: PASS — 683 total, 663 proven/delegated, 6 blocked,
  14 excluded; actionable 663/669 (99.1%).
- `go build ./...`: PASS.
- `go vet ./...`: PASS.
- `go test ./...`: PASS.
- `golangci-lint run ./...`: PASS — 0 issues.
- `gofumpt -l .`: PASS — clean.

## Carry-forward

- Branch: `glm/depth-appraise`.
- Base: main `f66f2947e` (ambush merged); the unrelated untracked brief
  `docs/briefs/BRIEF-2026-08-28-economy-specproc-cluster.md` remains untouched.
- Commit and open exactly one `glm/depth-auto` family PR after checking out
  main and pulling. Do not merge it unless every hosted check is green; if CI
  fails to fire, retry once with the prescribed workflow command and leave the
  PR open if it remains not-green.
