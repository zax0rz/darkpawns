# Depth-fidelity handoff — 2026-08-28 — `do_bite`

## Queue position

- The special-procedure inventory is exhausted, including the one permitted
  `objmagic.sleep-entry-gates` cast-sleep attempt.
- The interpreter-table sweep is now complete through `bite`: `ambush`,
  `appraise`, `auto`, `backstab`, `ban`, `bash`, `bearhug`, `behead`,
  `berserk`, `bite`, and the shared `do_not_here` family. The next family must
  be selected from a fresh main checkout, pull, frontier report, and C-table /
  manifest comparison.

## C path verified

- `src/interpreter.c:356` registers `bite` at `POS_RESTING` with `do_bite`.
- `src/new_cmds.c:1199-1300` first returns silently in a peaceful room, uses
  `one_argument`, silently falls back to `FIGHTING(ch)` for an empty argument,
  and emits the exact missing-target and self-target lines.
- Ordinary callers emit the C love-bite actor/victim/room act trio without
  damage. Flagged players must be transformed; higher immortals and player
  werewolf/vampire victims are refused.
- A transformed werewolf emits its act trio and calls
  `damage(ch, victim, MIN(15, level), SKILL_BITE)` followed by a two-pulse
  wait. A vampire draws `number(0, level / 2)`; only the zero branch damages
  and waits, while both vampire branches feed directly from eligible hunger
  conditions. `SKILL_BITE` selects the numbered skill-message set.

## Confirmed REDs and fixes

- The old Go path treated every bite as a generic damaging attack and emitted
  invented custom text. It now implements the C entry gates, ordinary love
  bite, werewolf branch, vampire branches, direct feeding arithmetic, and
  numbered post-damage skill message contract.
- `CmdBite` previously used `Bite who?`, attempted a room lookup for a
  fighting caller with no argument, and passed joined arguments to lookup. It
  now matches C punctuation, silent fallback, and `one_argument` parsing.
- The damage mapping now routes `bite` through attack type 150 (`SKILL_BITE`)
  instead of the unrelated generic bite type. No files under `src/` or
  `darkpawns-c-oracle/` were changed.

## Proof and gates

- `bite-depth --show-oracle --seed 1`: no normalized divergence; proves the
  no-argument, missing-target, self-target, and trailing-word love-bite path.
- `bite-groinrip-peaceful --show-oracle --seed 1`: no normalized divergence;
  proves bite's silent peaceful-room return alongside the existing groinrip
  control.
- Focused command and game tests pass for the fighting/no-argument gate,
  supernatural gates, werewolf contract, vampire feeding/RNG arms, and
  numbered skill-message fields.
- Full local gates pass: `make fidelity-depth` (800 total, 779
  proven/delegated, 6 blocked, 15 excluded; 99.2% actionable),
  `go build ./...`, `go vet ./...`, `go test ./...`,
  `golangci-lint run ./...`, and `make check-fmt`.
- PR #725 (`glm/depth-bite`) passed hosted lint, security, and test checks;
  build and deploy were skipped by CI. It was merged as `3d0300b60`.

## Carry-forward

- Preserve the unrelated untracked brief
  `docs/briefs/BRIEF-2026-08-28-economy-specproc-cluster.md`.
- Return to `main`, pull, rerun `make fidelity-depth`, reread
  `docs/fidelity/DEPTH_TESTING.md` and this newest handoff, then take the next
  un-manifested handler in `src/interpreter.c` table order.
