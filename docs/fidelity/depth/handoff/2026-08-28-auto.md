# Depth-fidelity handoff — 2026-08-28 — `do_auto`

## Queue position

- The special-procedure inventory and the single `objmagic.sleep-entry-gates`
  attempt are complete.
- The interpreter-table sweep has completed `ambush`, `appraise`, and now
  `auto` in order. `ask` and `auction` are already covered by the shared
  communication manifest. The next genuinely un-manifested family is
  `backstab` (`src/interpreter.c:343`, `do_backstab`).

## C path verified

- `src/interpreter.c:340` registers `auto` at `POS_DEAD` with `do_auto`.
- `src/act.other.c:1300-1412` checks the raw post-command argument, skips only
  leading spaces, compares the named preferences literally, toggles
  `PRF_AUTOEXIT`, `PRF_AUTOLOOT`, `PRF_AUTOGOLD`, and `PRF_AUTOSPLIT`, and emits
  the exact on/off bytes for each.
- The no-argument listing is a reachable but undefined C surface: its
  overlapping `sprintf(buf, "%s...", buf, ...)` writes are not defined by C;
  the existing breadth vehicle records the shipped Linux oracle's empty/unstable
  result and keeps that branch explicitly excluded rather than inventing a
  stable Go contract.

## RED finding and Go fix

- The C-first RED vehicle showed `auto EXIT` reaching
  `What do you want to make automatic?` in C while Go lowercased the argument
  and toggled exits.
- Go now skips only leading spaces and switches on the argument without case
  folding. The existing C-defined toggle behavior and player state transitions
  remain unchanged. No files under `src/` or `darkpawns-c-oracle/` were changed.

## Proof

- Existing `player-state-toggles` vehicle, with new depth annotations, proves
  both directions of exit, loot, gold, and split; the actor/observer topology
  also confirms the toggles are private to the actor.
- The same vehicle proves uppercase `EXIT` and an unknown argument both use the
  exact unknown-argument line without changing state.
- Seed 1 used `--show-oracle`; seeds 1, 2, 3, 5, and 8 all have no normalized
  divergence.
- Manifest: `docs/fidelity/depth/auto.tsv`, 7 rows — 6 proven and 1 explicit
  undefined-behavior exclusion.

## Gates

- `make fidelity-depth`: PASS — 690 total, 669 proven/delegated, 6 blocked,
  15 excluded; actionable 669/675 (99.1%).
- `go build ./...`: PASS.
- `go vet ./...`: PASS.
- `go test ./...`: PASS.
- `golangci-lint run ./...`: PASS — 0 issues.
- `gofumpt -l .`: PASS — clean.

## Carry-forward

- Branch: `glm/depth-auto`.
- Base: main `59015727d` (appraise merged); the unrelated untracked brief
  `docs/briefs/BRIEF-2026-08-28-economy-specproc-cluster.md` remains untouched.
- Commit and open exactly one `glm/depth-backstab` family PR after checking
  out main and pulling. Do not merge it unless every hosted check is green; if
  CI fails to fire, retry once with the prescribed workflow command and leave
  the PR open if it remains not-green.
