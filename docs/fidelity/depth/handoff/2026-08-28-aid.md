# Depth-fidelity handoff: `aid`

Date: 2026-08-28  
Branch: `glm/depth-aid`  
Starting main: `c37d6a59b` (`Merge pull request #711 from zax0rz/glm/depth-afk`)

## Frontier and queue position

The required main checkout/pull, `make fidelity-depth` checkpoint, and fresh
reads of `DEPTH_TESTING.md` and the `afk` handoff put the queue at the first
remaining un-manifested interpreter family, `aid` at `src/interpreter.c:327`.
The starting frontier was **631 total cases, 611 proven/delegated, 6 blocked,
and 14 excluded**: **611/617 actionable (99.0%)**.

This slice adds ten cases. The post-manifest frontier is **641 total cases,
621 proven/delegated, 6 blocked, and 14 excluded**: **621/627 actionable
(99.0%)**.

## C call path and branch inventory

The registered command is `{ "aid", POS_STANDING, do_first_aid, 0, 0 }` at
`src/interpreter.c:327`; dispatch reaches `src/new_cmds2.c:139-187`.
The reachable branches are the skill gate before argument parsing, no target,
missing room-visible target, self target, living target, random success,
immortal auto-success, and random failure. Success sets target HP to 1,
calls `update_pos`, sends distinct actor/victim/observer `act()` bytes,
improves first aid, and waits the target. Failure sends the actor line, uses
`TO_ROOM` for both target and observer, and applies
`WAIT_STATE(ch, PULSE_VIOLENCE+3)`.

## RED/GREEN proof and confirmed fixes

The C-first `aid-depth` vehicle proves no-argument, missing-target, self,
living-target, immortal-success, and success audience branches. The separate
`aid-failure-depth` vehicle proves the failure audience and padded wait path;
both use the required `DP_ORACLE_BIN` and `~dpclock pulse 20` controls. Both
vehicles matched the Go port on seeds **1, 2, 3, 5, and 8**.

The confirmed Go divergences were fixed without touching `src/` or
`darkpawns-c-oracle/`:

- use C's single `number(1, 101+victim_level)` draw and immortal override;
- restore target HP and position, target wait, and deferred skill improvement
  on success;
- preserve C's failure `TO_ROOM` target audience and exact failure text;
- carry the non-round `PULSE_VIOLENCE+3` wait through the skill result and
  raw player wait state;
- translate the C `skillset 'aid'` display name to Go's `first_aid` key; and
- support C's `set <player> hit <value>` vehicle field for the downed target.

Focused tests `TestDoFirstAidDepthSuccessState` and
`TestDoFirstAidDepthFailureState` pin the state transitions, deferred
improvement, exact wait, and audience flag. Existing
`TestCmdFirstAid_NoArgs` pins the skill-gate ordering.

## Gates

All required local gates pass on this branch:

- `make fidelity-depth` — exit 0;
- `go build ./...` — exit 0;
- `go vet ./...` — exit 0;
- `go test ./...` — exit 0;
- `golangci-lint run ./...` — `0 issues`; and
- `gofumpt -l .` — no output.

The unrelated untracked brief
`docs/briefs/BRIEF-2026-08-28-economy-specproc-cluster.md` remains preserved.

This handoff follows R1 (exact actor and audience bytes), R2 (registered
command and skillset surface), R3 (RNG and wait-state ordering), R4 (no
invented output), R5c (shared position/wait state), and R5e (actual C call-path
verification).

## Next queue

After this PR is handled, return to `main`, pull, rerun `make fidelity-depth`,
reread `DEPTH_TESTING.md` and this handoff, then take the next un-manifested
command family in interpreter table order: `alias`.
