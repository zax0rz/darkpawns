# Depth-fidelity handoff — 2026-08-28 — `do_bash`

## Queue position

- The special-procedure inventory and the single `objmagic.sleep-entry-gates`
  attempt are complete.
- The interpreter-table sweep has completed `ambush`, `appraise`, `auto`,
  `backstab`, `ban`, `do_not_here`, and now `bash` in table order. The next
  family must be selected from a fresh main checkout, pull, frontier report,
  and C-table/manifest comparison.

## C path verified

- `src/interpreter.c:347` registers `bash` at `POS_FIGHTING` with `do_bash`
  and the level-1 gate.
- `src/act.offensive.c:419-498` parses with `one_argument`, checks the skill
  and peaceful-room gates before target lookup, falls back to `FIGHTING(ch)`,
  then applies self, victim-position, mounted, movement, AC-adjusted roll,
  `MOB_NOBASH`, sleeping/immortal overrides, and the damage branches.
- A miss calls `damage(ch, vict, 0, SKILL_BASH)`, sets the caster sitting, and
  then receives the unconditional player two-pulse wait. A success calls the
  same damage path with `(GET_LEVEL(ch)/2)+1`, improves only on the player
  path, sits the victim, waits the victim two pulses, and applies the same
  player wait.
- The damage call owns the shared combat gates and the Bash set-132 message
  and RNG draw; those branches are delegated to the existing combat-entry and
  special-procedure manifests where appropriate.

## RED findings and Go fixes

- The C-first `bash-depth` vehicle found that `bash the Bashdepth` returned
  `Bash who?` in Go while C's `one_argument` resolved the first non-fill token
  and returned `Aren't we funny today...`. Go now uses the canonical parser.
- The C-first `bash-peaceful-depth` vehicle found that a peaceful room with a
  missing target returned `Bash who?` in Go while C emitted the peaceful-room
  line first. `CmdBash` now performs that command-layer check before target
  lookup.
- The guaranteed mortal-miss vehicle found Go appended an invented
  `You fall to the ground!` after C's set-132 line already said the caster
  toppled over. `sendSkillResult` now changes the caster position without
  adding that extra player-facing byte. No files under `src/` or
  `darkpawns-c-oracle/` were changed.

## Proof

- New vehicles: `bash-depth`, `bash-peaceful-depth`, and
  `bash-failure-depth`; existing `combat-skill-noskill` and
  `combat-bash-opener` now carry the no-skill and opener annotations.
- Seed 1 used `--show-oracle` for every new bash vehicle. The forced miss and
  legal opener both have no normalized divergence at seeds 1, 2, 3, 5, and 8.
- Focused tests cover fighting-target fallback, target/mount/movement gates,
  AC/damage formula, `MOB_NOBASH`, immortal auto-success, skill-message
  routing, target/caster state, waits, and miss/hit RNG draw order.
- Manifest: `docs/fidelity/depth/bash.tsv`, 21 rows — all proven or delegated.

## Gates

- `make fidelity-depth`: PASS — 740 total, 719 proven/delegated, 6 blocked,
  15 excluded; actionable 719/725 (99.2%).
- `go build ./...`: PASS.
- `go vet ./...`: PASS.
- `go test ./...`: PASS.
- `golangci-lint run ./...`: PASS — 0 issues.
- `gofumpt -l .` via `make check-fmt`: PASS — clean.

## Carry-forward

- Branch: `glm/depth-bash`.
- Base: main `ccfe0633a` (`do_not_here` merged); the unrelated untracked brief
  `docs/briefs/BRIEF-2026-08-28-economy-specproc-cluster.md` remains untouched.
- Open exactly one `glm/depth-bash` family PR. Do not merge it unless every
  hosted check is green; if CI fails to fire, retry once with the prescribed
  workflow command and leave the PR open if it remains not-green.
