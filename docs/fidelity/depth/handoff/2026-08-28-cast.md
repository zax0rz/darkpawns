# Depth-fidelity handoff — 2026-08-28 — command family `do_cast`

## Frontier and queue position

- Started from `main` at `cbc0f49cd`, pulled before work, refreshed the
  frontier with `make fidelity-depth`, and reread `docs/fidelity/DEPTH_TESTING.md`
  plus the prior dated handoff.
- The special-procedure census and the one permitted
  `objmagic.sleep-entry-gates` cast-sleep attempt are exhausted. The command
  sweep reached `do_cast`: `cast` at `src/interpreter.c:373` and `will` at
  `src/interpreter.c:825`. The next distinct unmanifested family must be
  selected fresh from the interpreter table after this slice.
- Before this slice: 840 total cases, 819 proven/delegated, 6 blocked, and 15
  excluded. After this slice: 862 total, 840 proven/delegated, 6 blocked, and
  16 excluded; actionable completion is 840/846 (99.3%).

## C path and reachability

R5e was verified from the actual call path: the player registrations in
`src/interpreter.c:373,825` enter `do_cast` in `src/spell_parser.c:916-1110`;
spell lookup and multiword token abbreviation are implemented by
`find_skill_num` at `src/spell_parser.c:370-392`; and the cast contract enters
`cast_spell` at `src/spell_parser.c:879-925`. Shared spell effects and damage
remain owned by the existing spell/object-magic and magic-user depth slices.

The safe live vehicle uses an Implementor and an in-room level-1 peer. It
proves blank, `will`, unquoted, unknown, unmatched-opening-quote,
missing-target, and protected low-level target branches. Successful casts are
padded with `~dpclock pulse 20` as required by the procedure.

## Proof and confirmed divergences

- RED on the pre-fix implementation: `will` used the generic unknown-command
  surface; C accepts a nonempty spell remainder after an unmatched opening
  quote while Go rejected it; Go passed the complete trailing target string
  instead of C's first `one_argument`; and Go spell damage bypassed C's
  low-level non-outlaw player protection.
- Additional C branches were enumerated and covered with unit proof: literal
  mortal-psionic `cast` refusal, power wording, smartness, charm-master,
  self-only/not-self/group restrictions, target ordering/defaults, mana and
  weight gates, concentration failure, and successful `CallMagic` boundary.
- GREEN on `cast-depth`, seeds 1, 2, 3, 5, and 8, with no normalized
  divergence. Focused session/game/spells tests pass.

## Change and gates

- Go-only fixes are in `pkg/session/cast_cmds.go`, `pkg/game/practice.go`,
  `pkg/game/affects_constants.go`, and `pkg/spells/damage_spells.go`, with
  focused tests, scenario, and manifest updates. No `src/` or C-oracle files
  were edited.
- The manifest adds 22 `do_cast` cases, including one unreachable NPC branch
  explicitly excluded and one delegated shared damage/effect boundary.
- The slice must be committed and submitted as one `glm/depth-cast` PR. Merge
  only if every GitHub check is green; if CI does not fire, retry once with
  `gh workflow run "Dark Pawns CI/CD" --ref <branch>`, then leave the PR open
  if it is still not green.
- Fidelity rules exercised: R1, R2, R3, R4, R5c, and R5e.

## Next action

Checkout and pull `main`, rerun `make fidelity-depth`, reread
`DEPTH_TESTING.md` and this handoff, then select the next unclaimed distinct
command family in `src/interpreter.c` table order. Keep the blocked sleep row
blocked unless a later queue instruction changes its status.
