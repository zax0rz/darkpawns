# Depth-fidelity handoff: `afk`

Date: 2026-08-28  
Branch: `glm/depth-afk`  
Starting main: `9e1bc70c5` (`Merge pull request #710 from zax0rz/glm/depth-admobs`)

## Frontier and queue position

The required main checkout/pull and `make fidelity-depth` checkpoint before
this slice reported **627 total cases, 607 proven/delegated, 6 blocked, and 14
excluded**: **607/613 actionable (99.0%)**. The first remaining
un-manifested interpreter command family was `afk`, immediately after
`admobs` at `src/interpreter.c:326`.

This slice adds four cases. The post-manifest frontier is **631 total cases,
611 proven/delegated, 6 blocked, and 14 excluded**: **611/617 actionable
(99.0%)**.

## C call path and branch inventory

The C command row is `{ "afk", POS_DEAD, do_afk, 0, 0 }` at
`src/interpreter.c:326`; normal command dispatch reaches
`src/act.other.c:1283-1300`. The handler has two reachable state branches:
when `PRF_AFK` is set it clears the bit, sends the return text to the actor,
and sends the return act to the room; otherwise it sets the bit, sends the AFK
text to the actor, and sends the AFK act to the room. Command arguments are
ignored.

## RED/GREEN proof

This was a pure-coverage depth round: the existing Go port was already
faithful, so there was no source divergence to fix. The new `afk-depth`
vehicle was run with `--show-oracle`, confirming the intended C handler, and
matched the actor and named peer bytes for enable, disable, argument-bearing
toggle, and prompt framing on seeds **1, 2, 3, 5, and 8**. The focused
`TestDoAFKToggleState` test verifies the typed AFK state, the C `PRF_AFK` bit,
and the empty AFK message across both transitions. No `src/` or
`darkpawns-c-oracle/` file was edited.

## Manifest and gates

`docs/fidelity/depth/afk.tsv` records four proven cases: three oracle audience
and entry cases plus one focused state case.

The full local gates pass:

- `make fidelity-depth`;
- `go build ./...`;
- `go vet ./...`;
- `go test ./...`;
- `golangci-lint run ./...` — `0 issues`; and
- `gofumpt -l .` — no output.

This handoff follows R1 (exact actor and room bytes), R2 (the registered C
command surface), R3 (seed-matrix stability), R4 (argument ignorance and no
invented output), R5c (shared AFK state/act path), and R5e (actual C
call-path verification).

## Next queue

After this PR is handled, return to `main`, pull, rerun `make fidelity-depth`,
reread `DEPTH_TESTING.md` and this handoff, then take the next un-manifested
command family in interpreter table order: `aid`.
