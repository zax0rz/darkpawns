# Dated Handoff: 2026-08-27 (obj-index fixture round)

- New harness fixture `add-obj-index <file.obj>` appends an otherwise-
  unindexed obj file to BOTH trees' disposable obj/index — inserted at its
  NUMERIC position, not appended: C's real_object() binary-searches
  obj_index, which index_boot builds in index-file order, so an appended
  131.obj (vnums 13100+) after 301.obj breaks the sort and the zone loader
  rejects the spawn with "Invalid vnum, cmd disabled" (observed live).
- Two more object-magic rows converted oracle-green-multiseed:
  - objmagic.blindness.messages (quaff-blindness): 131.obj's plaid potion
    13103 (mass-heal + blindness). 8 seeds, both arms (fail trio / the
    caster-only save fade line).
  - objmagic.curse.messages (recite-curse): 58.obj's return scroll 5804
    (curse + word of recall). 6 seeds, both arms (fail trio / NOEFFECT),
    plus the full word-of-recall teleport with room look.
- Two more confirmed divergences fixed (found by the fixture, Go-only):
  1. MagUnaffects was a half-port: no unaffecting guard (it sent an
     invented "Your vision clears!" on EVERY heal/mass-heal — C stays
     silent for heal/mass-heal and NOEFFECTs the caster for the rest),
     wrong cure text (C: "Your vision returns!"), no SMOKESCREEN pairing,
     no to_room lines, no NOEFFECT at all. Now a faithful mag_unaffects
     port (magic.c:1828-1876); TestMagUnaffects pins guards + bytes.
  2. The scroll-recite word-of-recall path emitted the invented "Recall
     failed: world interface not available." — the spells package's
     worldTransfer interface demanded PlayerTransfer(interface{}, ...) but
     *game.World's methods take concrete *Player, so the assertion could
     never succeed. castWordOfRecall now delegates to
     game.ExecuteWordOfRecall, a faithful spell_recall port (spells.c:
     124-165: BFR gates with the SPELL's bytes — which differ from the
     recall command's — "$n disappears."/"$n appears" pair, hometown
     2/3/else transfer, unmount, look landing). NOTE: castTeleport still
     has the same never-matching transferWorld assertion (line ~2406) —
     teleport via scroll/cast will need the same adapter treatment.
- Sleep/chill-touch/flamestrike save arms remain unit-green (no vehicles
  in indexed OR indexable obj files found this round).
- Prior handoff notes on pre-existing REDs stand: guild-practice and
  backstab-aware-trio diverge on main before and after this wave.
