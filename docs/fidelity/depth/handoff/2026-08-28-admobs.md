# Depth-fidelity handoff: `admobs`

Date: 2026-08-28  
Branch: `glm/depth-admobs`  
Starting main: `7cb535055` (`Merge pull request #709 from zax0rz/glm/depth-advance`)

## Frontier and queue position

The required main checkout/pull and `make fidelity-depth` checkpoint before
this slice reported **623 total cases, 603 proven/delegated, 6 blocked, and 14
excluded**: **603/609 actionable (99.0%)**. The first remaining
un-manifested interpreter command family was `admobs`, at
`src/interpreter.c:325`.

This slice adds four cases. The post-manifest frontier is **627 total cases,
607 proven/delegated, 6 blocked, and 14 excluded**: **607/613 actionable
(99.0%)**.

## C call path and branch inventory

The C command row is `{ "admobs", POS_DEAD, adjust_mobs, LVL_IMPL-1, 0 }` at
`src/interpreter.c:325`. `command_interpreter` performs prefix resolution and
the level-39 gate before dispatching to `adjust_mobs`
(`src/interpreter.c:1407-1456`). The handler is
`src/olc.c:279-307`.

`adjust_mobs` ignores its argument and loops through every mob prototype. For
each one it sets `damroll` to `level / 2` through level 10, otherwise the
integer conversion of `level / 1.50`; it sets the hit-point addend to
`10*level+10`, adds `13*(level-22)` above level 22, and adds
`560*(level-30)` above level 30. It then marks the prototype's zone for a
mobile OLC save and emits only `Okay.\r\n`. There are no other player-facing
handler branches.

## RED/GREEN proof and confirmed fixes

On clean main, seed 1 was RED: C returned `Okay.\r\n`, while Go returned
`Huh?!?\r\n` because the C command had no Go registration. The Go-only fix
registers `admobs` with the existing authoritative level-39 gate and applies
the C scaling to Go's corresponding `Damage.Plus` and `HP.Plus` prototype
fields under one world lock. Go has no dirty-zone OLC queue, so the operation
changes in-memory prototypes only; it does not invent persistence behavior.

The `admobs-depth` vehicle is GREEN on seeds **1, 2, 3, 5, and 8**, with
`--show-oracle` confirming the intended C handler. It proves the ignored
argument, repeat idempotence, and exact `Okay.` bytes. Focused tests prove the
level-39 gate and the complete scaling formula at levels 0, 10, 11, 22, 23,
30, 31, and 40 while preserving dice shapes. No `src/` or
`darkpawns-c-oracle/` file was edited.

## Manifest and gates

`docs/fidelity/depth/admobs.tsv` records four proven cases: two oracle command
cases and two focused state/gate cases.

The full local gates pass:

- `make fidelity-depth`;
- `go build ./...`;
- `go vet ./...`;
- `go test ./...`;
- `golangci-lint run ./...` — `0 issues`; and
- `gofumpt -l .` — no output.

This handoff follows R1 (exact player-facing bytes), R2 (the registered C
command surface and level gate), R3 (seed-matrix stability), R4 (no invented
argument or persistence output), R5c (shared prototype representation), and
R5e (actual C call-path verification).

## Next queue

After this PR is handled, return to `main`, pull, rerun `make fidelity-depth`,
reread `DEPTH_TESTING.md` and this handoff, then take the next un-manifested
command family in interpreter table order: `afk`.
