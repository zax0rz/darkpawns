# Depth-fidelity handoff — 2026-08-28 — `do_bearhug`

## Queue position

- The special-procedure inventory and the single `objmagic.sleep-entry-gates`
  attempt are complete.
- The interpreter-table sweep has completed `ambush`, `appraise`, `auto`,
  `backstab`, `ban`, `do_not_here`, `bash`, and now `bearhug` in table order.
  The next family must be selected from a fresh main checkout, pull, frontier
  report, and C-table/manifest comparison.

## C path verified

- `src/interpreter.c:348` registers `bearhug` at `POS_FIGHTING` with
  `do_bearhug` and the level-1 gate.
- `src/new_cmds.c:471-543` parses with `one_argument`, checks the skill and
  mounted gates before target lookup, falls back to `FIGHTING(ch)`, then
  applies the immortal-target, self-target, bare-hand, sleeping-target,
  immortal-caster, and `MOB_NOBASH` branches.
- The C roll is `number(1,150)` against the player's skill on the command
  path. A miss calls `damage(ch, victim, 0, SKILL_BEARHUG)`; a hit calls the
  same path with truncated `GET_LEVEL(ch)*1.5`, then improves the skill. Both
  paths use skill-message set 142, start combat, and end with the player's
  two-pulse wait.

## RED findings and Go fixes

- The original Go command joined all arguments and did not implement C's
  mounted-before-lookup or fighting-target fallback order. It now uses the
  canonical `OneArgument` parser, checks mounted state first, and resolves the
  fighting target when the named lookup fails.
- The original Go skill path had an invented movement gate and did not mirror
  C's immortal-target/self/wielded ordering, sleeping/immortal/NOBASH forced
  failure, set-142 damage routing, combat enrollment, wait, or deferred
  improvement. Those confirmed divergences are now aligned; no files under
  `src/` or `darkpawns-c-oracle/` were changed.

## Proof

- New vehicles: `bearhug-depth`, `bearhug-mounted-depth`,
  `bearhug-failure-depth`, and `bearhug-outcome-depth`; the shared
  `combat-skill-noskill` vehicle now also credits `bearhug.no-skill`.
- The mounted vehicle first rides the legal horse and then proves the exact
  `Dismount first!` branch. The failure and success vehicles have no
  normalized divergence at seeds 1, 2, 3, 5, and 8, including combat pulses.
- Focused tests cover parser fallback, gate ordering, bare-hand equipment,
  the absence of a movement gate, forced-failure branches, set-142 result
  routing, truncated damage arithmetic, combat start, improvement, and wait
  state.
- Manifest: `docs/fidelity/depth/bearhug.tsv`, 18 rows — all proven or
  delegated.

## Gates

- `make fidelity-depth`: PASS — 737 total, 716 proven/delegated, 6 blocked,
  15 excluded; actionable 716/722 (99.2%).
- Focused bearhug tests: PASS.
- Full repository gates remain to be run on the rebased family branch before
  opening the PR.

## Carry-forward

- The bash family PR has merged with all required hosted checks green as
  merge commit `5fab7ec4d`; rebase this bearhug work onto the resulting main
  before the final gate run.
- Preserve the unrelated untracked brief
  `docs/briefs/BRIEF-2026-08-28-economy-specproc-cluster.md`.
- Open exactly one `glm/depth-bearhug` family PR. Do not merge it unless every
  hosted check is green; if CI does not fire, retry once with the prescribed
  workflow command and leave the PR open if it remains not-green.
