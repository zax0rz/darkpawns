# Depth-fidelity handoff — 2026-08-28 — `do_berserk`

## Queue position

- The special-procedure inventory and the single `objmagic.sleep-entry-gates`
  attempt are complete.
- The interpreter-table sweep has completed `ambush`, `appraise`, `auto`,
  `backstab`, `ban`, `bash`, `bearhug`, `behead`, `do_not_here`, and now
  `berserk` in table order. The next family must be selected from a fresh main
  checkout, pull, frontier report, and C-table/manifest comparison.

## C path verified

- `src/interpreter.c:353` registers `berserk` at `POS_FIGHTING` with
  `do_berserk` and the level-1 gate.
- `src/new_cmds.c:2134-2187` checks the player's skill, draws
  `number(1,101)` before the already-affected gate, applies the strict
  immortal bypass, compares the player skill, emits the exact success/failure
  text, unconditionally calls `improve_skill()` because of C's dangling else,
  installs three duration-1 `AFF_BERSERK` records, and applies
  `WAIT_STATE(ch, PULSE_VIOLENCE * 2)`.
- `src/spec_procs.c:509-535` can call the same C procedure through the fighter
  SPECIAL with `subcmd=1`; that native mob path is already proven and delegated
  to `mob.fighter-berserk` in the special-procedure manifest.

## RED findings and Go fixes

- The existing Go implementation already matched the command's visible
  learned-skill success, failure, affect, wait, immortal, and improvement
  behavior. The C-first vehicles `berserk-depth` and
  `berserk-failure-depth` had no normalized divergence at seed 1, with the
  failure vehicle forcing a second mortal target so the primary God actor did
  not take C's immortal bypass.
- The audit found one draw-order divergence in the already-affected branch:
  Go rejected before drawing percent while C draws first. Go now consumes the
  `number(1,101)` equivalent before the affect gate, and
  `TestDoBerserk_AffectedGateConsumesPercentDraw` pins the following RNG
  position. No files under `src/` or `darkpawns-c-oracle/` were changed.

## Proof

- Existing `mortal-batch6` now carries the no-skill annotation. New vehicles
  `berserk-depth` and `berserk-failure-depth` cover ignored arguments, ordinary
  success, and ordinary failure; both were run with `--show-oracle` at seed 1
  with no normalized divergence.
- Focused tests cover the already-affected draw order, immortal bypass,
  success and failure affect records, wait state, and unconditional skill
  improvement.
- Manifest: `docs/fidelity/depth/berserk.tsv`, 12 rows — all proven or
  delegated.

## Gates

- Run the full local gates before opening the family PR:
  `make fidelity-depth`, `go build ./...`, `go vet ./...`, `go test ./...`,
  `golangci-lint run ./...`, and `make check-fmt`.
- Hosted checks must all be green before merge. If CI does not fire, retry once
  with `gh workflow run "Dark Pawns CI/CD" --ref glm/depth-berserk`; if it is
  still not green, leave the PR open and advance.

## Carry-forward

- Branch: `glm/depth-berserk`.
- Base: main `4c593efcf` (behead merged); preserve the unrelated untracked
  brief `docs/briefs/BRIEF-2026-08-28-economy-specproc-cluster.md`.
- Open exactly one `glm/depth-berserk` family PR. Do not merge it unless every
  hosted check is green.
