# Depth-fidelity handoff — 2026-08-28 — `do_gen_write`

## Queue position

- The special-procedure inventory is exhausted, including the one permitted
  `objmagic.sleep-entry-gates` cast-sleep attempt.
- The interpreter-table sweep is complete through `do_gen_tog` and now
  `do_gen_write`. The next family must be selected from a fresh main checkout,
  pull, frontier report, and C-table / manifest comparison.

## C path verified

- `src/interpreter.c:370` registers `bug` with `SCMD_BUG`, `:510` registers
  `idea` with `SCMD_IDEA`, `:780` registers `todo` with `SCMD_TODO` and
  `LVL_GOD-1`, and `:789` registers `typo` with `SCMD_TYPO`; all share
  `do_gen_write` at `src/act.other.c:1079-1127`.
- The C subcommand switch runs first and returns silently for an unknown
  subcommand. NPC callers then receive `Monsters can't have ideas - Go away.`
  and return. Player callers skip leading whitespace, delete doubled dollars,
  reject empty input, append the C-formatted record to the selected
  `misc/bugs`, `misc/typos`, `misc/ideas`, or `misc/todo` file, report the
  exact open failure when needed, and otherwise receive the shared
  `Okay.  Thanks!` response.

## Confirmed REDs and fixes

- Main-branch focused REDs showed that Go silently discarded the NPC branch,
  emitted invented per-command success text, wrote a non-C record format,
  preserved doubled dollars, treated whitespace-only input as success, and
  responded to an unknown subcommand.
- Go now follows the C branch order, NPC and open-failure bytes, leading-space
  and doubled-dollar normalization, C report record format, and shared success
  response. No files under `src/` or `darkpawns-c-oracle/` were changed.

## Proof and gates

- `gen-write-depth --show-oracle --seed 1` is green with no normalized
  divergence for `bug`, `typo`, `idea`, and privileged `todo` no-argument
  calls. The live vehicle deliberately avoids successful report writes because
  the C handler appends to relative `misc/*` files in its oracle working tree;
  focused Go tests use a temporary working directory for the equivalent
  success, record, NPC, entry-gate, and open-failure branches.
- Focused `TestDoGenWrite*` tests pass.
- Final local frontier: `make fidelity-depth` reports 840 total, 819
  proven/delegated, 6 blocked, and 15 excluded; actionable completion is
  819/825 (99.3%).
- Full local gates pass: `go build ./...`, `go vet ./...`, `go test ./...`,
  `golangci-lint run ./...` (0 issues), and `gofumpt -l .` (clean).

## Carry-forward

- Preserve the unrelated untracked brief
  `docs/briefs/BRIEF-2026-08-28-economy-specproc-cluster.md`.
- Return to `main`, pull, rerun `make fidelity-depth`, reread
  `docs/fidelity/DEPTH_TESTING.md` and this newest handoff, then take the next
  un-manifested interpreter-table family in order.
