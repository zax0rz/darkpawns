# Depth-fidelity handoff — 2026-08-28 — `do_ban`

## Queue position

- The special-procedure inventory and the single `objmagic.sleep-entry-gates`
  attempt are complete.
- The interpreter-table sweep has completed `ambush`, `appraise`, `auto`,
  `backstab`, and now `ban` in table order. The next family must be selected
  from a fresh main checkout, pull, frontier report, and C-table/manifest
  comparison.

## C path verified

- `src/interpreter.c:345` registers `ban` at `POS_DEAD` with `do_ban` and
  `LVL_GRGOD` authority.
- `src/ban.c:132-210` covers empty-list output, fixed-column listing, the
  `two_arguments` flag/site parse, ALL/SELECT/NEW validation, duplicate-site
  refusal, 50-byte site storage truncation, head insertion, and the success
  message/write path.
- `src/ban.c:86-104` uses literal `strstr` matching for `isbanned`; it does not
  interpret `*` as a wildcard.

## RED findings and Go fixes

- The C-first `ban-depth` vehicle found four command divergences: Go listed
  oldest-first, formatted dates as `YYYY-MM-DD`, failed to skip C fill words
  in the site token, and returned an implementation error string for duplicate
  sites.
- Go now mirrors C head-first list state, `Mon Jan 02` list dates, canonical
  `one_argument` parsing for both tokens, exact duplicate text, and the
  `BANNED_SITE_LENGTH` truncation before later duplicate comparison.
- The C source audit also confirmed Go's wildcard matching was invented; the
  login/site-check path now uses literal substring matching, and its focused
  test records that rule. No files under `src/` or `darkpawns-c-oracle/` were
  changed.

## Proof

- New vehicle: `cmd/dp-oracle-diff/scenarios/ban-depth.txt` proves empty-list,
  add, fixed list format/date, newest-first ordering, fill-word and trailing
  argument parsing, duplicate, invalid flag, missing site, and 50-byte
  truncation. Seed 1 used `--show-oracle`; seeds 1, 2, 3, 5, and 8 all have no
  normalized divergence.
- Focused `TestIsBannedLiteralAsterisk` proves the C `strstr` behavior.
- Manifest: `docs/fidelity/depth/ban.tsv`, 10 rows — all proven.

## Gates

- `make fidelity-depth`: PASS — 716 total, 695 proven/delegated, 6 blocked,
  15 excluded; actionable 695/701 (99.1%).
- `go build ./...`: PASS.
- `go vet ./...`: PASS.
- `go test ./...`: PASS.
- `golangci-lint run ./...`: PASS — 0 issues.
- `gofumpt -l .`: PASS — clean.

## Carry-forward

- Branch: `glm/depth-ban`.
- Base: main `5d91f3daa` (backstab merged); the unrelated untracked brief
  `docs/briefs/BRIEF-2026-08-28-economy-specproc-cluster.md` remains untouched.
- Open exactly one `glm/depth-ban` family PR. Do not merge it unless every
  hosted check is green; if CI fails to fire, retry once with the prescribed
  workflow command and leave the PR open if it remains not-green.
