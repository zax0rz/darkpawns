# Depth-fidelity handoff — `think` — 2026-09-03

## Scope

This is the first file-order residual from the off-command surface inventory.
The branch starts from merged `main` at `28616693e`, whose depth report is
4,111 total cases: 4,014 proven/delegated, 46 blocked, and 51 excluded. The
inventory records `src/act.comm.c` as a residual family because `do_think`
had no independent oracle row even though the Go handler and unit tests were
already present.

## C path and vehicle

The command table registers `think` at `src/interpreter.c:766` with
`POS_RESTING`; `src/act.comm.c:1356-1386` skips leading spaces, emits the
no-argument actor/room pair, and for a nonempty argument checks
`PLR_NOSHOUT`/`GET_INT`, then emits the room thought and actor echo (or
`Ok.` under `PRF_NOREPEAT`). The new vehicle
`cmd/dp-oracle-diff/scenarios/think-depth.txt` uses an actor and co-located
peer, with no RNG or mutable fixture.

The seed-1 `--show-oracle` run reported no normalized divergence. The exact
blocks were:

```text
You think about life, the universe, and everything.
Mordecai thinks about life, the universe, and everything.
You think . o O ( about tulips )
Mordecai thinks . o O ( about tulips )
```

The actor and peer blocks match byte-for-byte between C and Go. Existing unit
tests additionally cover the no-shout gate, zero-intelligence gate, and
no-repeat actor acknowledgement. No Go behavior change was needed and no
oracle source was edited.

## Result

Add `think.no-argument` and `think.spoken-thought` to `docs/fidelity/depth/think.tsv`
as `oracle-green` and promote the `src/act.comm.c` activity family in
`surface-inventory.tsv` to `proven-already`, since all of its player-facing
handler families now have an owning depth proof or explicitly owned exclusion.

This slice applies R1/R2/R4/R5c/R5e: preserve exact audience bytes, honor the
registered command gate, do not infer unrelated communication coverage, avoid
duplicating shared owners, and verify the actual C call path.

