# Depth-fidelity handoff — 2026-08-28 — command family `alias`

## Frontier and queue position

- Started from `main` at `77479fad7` (the merged `aid` slice), refreshed the
  frontier, and re-read `docs/fidelity/DEPTH_TESTING.md` and the latest prior
  handoff before work.
- The special-procedure inventory is exhausted: its remaining gaps are already
  explicitly blocked or excluded in `spec-procs.tsv`. The next standing queue
  item is the one blocked `objmagic.sleep-entry-gates` row; the interpreter
  command-family sweep follows it.
- Before this slice: 641 total cases, 621 proven/delegated, 6 blocked, and 14
  excluded. After this slice: 654 total, 634 proven/delegated, 6 blocked, and
  14 excluded; actionable completion is 634/640 (99.1%).
- This slice claims the next unmanifested interpreter-table family, `alias`.

## C call path and branch inventory

R5e was verified from the actual C path. `src/interpreter.c:328` registers
`alias` at `POS_DEAD`; `do_alias` at `src/interpreter.c:981-1038` handles empty
listing, missing deletion, add/redefine, reserved-name rejection, doubled-dollar
collapse, 120-byte truncation, and simple/complex classification.
`perform_complex_alias` at `src/interpreter.c:1049-1100` splits semicolon
commands and substitutes `$1..$9` and `$*`; `perform_alias` at
`src/interpreter.c:1102-1141` performs simple replacement before
`command_interpreter`, while `src/comm.c:603-628` prevents recursive expansion
of queued alias commands.

## Proof and confirmed divergences

- The initial `alias-depth` RED proved that Go used non-C delete/list messages
  and list formatting, retained original arguments for simple aliases, allowed
  the reserved `alias` trigger, reported `Alias updated.`, and treated complex
  aliases as one unexpanded command.
- The Go implementation now matches C's exact manager bytes, head insertion on
  redefine, reserved-name gate, doubled-dollar normalization, truncation
  warning, alias type classification, simple argument discard, semicolon
  ordering/framing, `$1` positional substitution, `$*` glob substitution, and
  non-recursive complex expansion.
- Added `cmd/dp-oracle-diff/scenarios/alias-depth.txt` with eleven live cases;
  the vehicle is green on seeds 1, 2, 3, 5, and 8. Focused unit tests cover the
  expansion class plus manager truncation and doubled-dollar state.

## Change and gates

- Go-only changes landed in `pkg/game/aliases.go`, `pkg/session/act_social.go`,
  `pkg/session/commands.go`, and the input-drain marker in
  `pkg/session/manager.go`; no C or oracle source was edited.
- Manifest: `docs/fidelity/depth/alias.tsv` (13 rows).
- PR #713 (`glm/depth-alias`) was self-merged only after hosted test, lint, and
  security checks all passed. Merge landed on `main` as `3414cdcb2`.
- Passed `make fidelity-depth`, `go build ./...`, `go vet ./...`, `go test
  ./...`, `golangci-lint run ./...`, and clean `gofumpt -l .`; `git diff --check`
  was clean.
- Fidelity rules exercised: R1, R2, R3, R4, R5c, and R5e.

## Next action

Checkout and pull `main`, rerun `make fidelity-depth`, reread
`DEPTH_TESTING.md` and this handoff, then attempt the single blocked
`objmagic.sleep-entry-gates` row once through the cast-sleep outlaw/reagent
vehicle. Convert only the branches that vehicle reaches and leave the rest
blocked with sharp notes. After that, begin the remaining un-manifested
interpreter command families in source-table order.
