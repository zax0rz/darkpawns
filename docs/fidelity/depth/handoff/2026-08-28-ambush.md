# Depth-fidelity handoff — 2026-08-28 — `do_ambush`

## Queue position

- Special-procedure inventory and the single `objmagic.sleep-entry-gates`
  attempt are complete.
- This session advanced the first un-manifested interpreter family after
  `do_flesh_alter`: `ambush` (`src/interpreter.c:332`, `do_ambush`).
- The next un-manifested family in interpreter table order is `appraise`.

## C path verified

- `src/act.offensive.c:1379-1451` owns `ambush_event`.
- `src/act.offensive.c:1454-1506` owns the command gates and schedules the
  event at `PULSE_VIOLENCE*2`.
- The event clears `GET_ACTION`, refuses a fighting actor, checks the saved
  room and current victim room, then draws `number(1,131)`.
- `MOB_AWARE` forces the failure arm after the draw. Success computes strength
  damage + damroll + wielded dice + truncated `level*2.6`, then adds truncated
  hidden damage, calls `damage(..., SKILL_AMBUSH)`, improves the skill, and
  waits both victim and actor.
- `fight.c:1300-1718` and `lib/misc/messages` key 191 were followed through
  the shared damage/death path. The C order is skill message, death line,
  XP, then death cry for the lethal mob vehicle.

## Proof

- `ambush-depth`: seed 1 with `--show-oracle`; seeds 1, 2, 3, 5, and 8 all
  have no normalized divergence.
- `ambush-entry-gates`: city/forest-sector refusal fixture; seed 1 with
  `--show-oracle` is green.
- `ambush-lost-prey`: actor leaves during the delayed action; seed 1 with
  `--show-oracle` is green.
- Focused tests in `pkg/game/ambush_depth_test.go` cover the 40-pulse delay,
  silent fighting abort, lost prey, AWARE forced failure and mutual combat,
  weapon/strength/level/hidden arithmetic, waits, and hide removal.
- Manifest: `docs/fidelity/depth/ambush.tsv`, 12 rows, all proven or delegated.

## Go changes

- Added a typed runtime ambush action slot on `Player` and a real event-queue
  implementation in `pkg/game/ambush.go`.
- Routed ambush resolution through the shared C damage path with attacktype
  191 and a World-owned mob death callback.
- Corrected C `AFF_HIDE` bit removal, death-line capitalization, and position
  transition in the shared damage seam; suppressed the Go-only corpse
  announcement for attacktype 191.
- No files under `src/` or `darkpawns-c-oracle/` were edited.

## Gates

- `make fidelity-depth`: PASS — 676 total, 656 proven/delegated, 6 blocked,
  14 excluded; actionable 656/662 (99.1%).
- `go build ./...`: PASS.
- `go vet ./...`: PASS.
- `go test ./...`: PASS.
- `golangci-lint run ./...`: PASS — 0 issues.
- `gofumpt -l .`: PASS — clean.

## Carry-forward

- Branch: `glm/depth-ambush`.
- Base: main `023a4207e` (flesh-alter merged); the unrelated untracked brief
  `docs/briefs/BRIEF-2026-08-28-economy-specproc-cluster.md` remains untouched.
- Open the one-family PR only after committing this handoff. Do not merge
  until every hosted check is green; if CI does not fire, retry once with the
  prescribed workflow command and leave the PR open if it remains not-green.
