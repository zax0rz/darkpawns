# Depth-fidelity handoff: `advance`

Date: 2026-08-28  
Branch: `glm/depth-advance`  
Starting main: `7c8674177` (`Merge pull request #708 from zax0rz/glm/depth-at`)

## Frontier and queue position

The required main checkout/pull and `make fidelity-depth` checkpoint before
this slice reported **611 total cases, 591 proven/delegated, 6 blocked, and 14
excluded**: **591/597 actionable (99.0%)**. The first remaining
un-manifested interpreter command family was `advance`, immediately after
`at` at `src/interpreter.c:324`.

This slice adds twelve cases. The post-manifest frontier is **623 total cases,
603 proven/delegated, 6 blocked, and 14 excluded**: **603/609 actionable
(99.0%)**.

## C call path and branch inventory

The C command row is `{ "advance", POS_DEAD, do_advance, LVL_GRGOD, 0 }` at
`src/interpreter.c:324`. `command_interpreter` performs prefix resolution and
the `LVL_GRGOD` gate before calling `do_advance`
(`src/interpreter.c:1407-1456`). The handler is
`src/act.wizard.c:1480-1580`; target visibility and ordinal resolution come
from `get_char_vis` in `src/handler.c:1263-1379`.

The verified C branches are: missing target; target not visible; actor
authority versus target level; NPC rejection; missing, zero, malformed, or
too-high level; a requested level above the actor; already-at-level; ordinary
promotion; promotion through the Implementor boundary; and demotion. The
promotion path uses `gain_exp_regardless` and `advance_level`
(`src/limits.c:327-363`, `src/class.c:600-720`). Demotion recursively runs
`do_start` and then promotes back to the requested level.

## RED/GREEN proof and confirmed fixes

On clean main, seed 1 was RED: Go returned `Advance who?` instead of C's
`That player is not here.` for a missing target, used invented `Invalid level.`
text, accepted zero and level 41, and mutated targets through those rejected
branches. The first NPC probe used an invalid non-ordinal name; C's actual
`get_char_vis` path was rechecked and the vehicle was corrected to `1.orc`.
The valid promotion probe then exposed an additional Go deadlock entering the
immortal boundary and extra blank transport lines between repeated rise
messages.

The Go-only fix mirrors C's branch order, `atoi`-style leading-digit parsing,
visible-character lookup, exact messages, promotion/demotion state changes,
and TO_VICT output. It fixes the nested lock in `AdvanceLevel` while preserving
the C immortal perks, and batches repeated promotion announcements so the
player-facing byte stream is contiguous. No `src/` or
`darkpawns-c-oracle/` file was edited.

The `advance-depth` vehicle is GREEN on seeds **1, 2, 3, 5, and 8**, including
the level-1-to-40 promotion and demotion state paths. Focused authority tests
cover the actor-level and requested-level gates.

## Manifest and gates

`docs/fidelity/depth/advance.tsv` records all twelve cases as proven: ten
oracle cases, one multiseed promotion case, and two focused unit cases.

The full local gates pass:

- `make fidelity-depth`;
- `go build ./...`;
- `go vet ./...`;
- `go test ./...`;
- `golangci-lint run ./...` — `0 issues`; and
- `gofumpt -l .` — no output.

This handoff follows R1 (exact player-facing bytes), R2 (the registered C
command surface and gate), R3 (deterministic promotion output), R4 (removal
of invented behavior), R5c (canonical target resolution), and R5e (actual C
call-path verification).

## Next queue

After this PR is handled, return to `main`, pull, rerun `make fidelity-depth`,
reread `DEPTH_TESTING.md` and this handoff, then take the next un-manifested
command family in interpreter table order: `admobs`.
