# Depth-fidelity handoff: `at`

Date: 2026-08-28  
Branch: `glm/depth-at`  
Starting main: `374d90d26` (`Merge pull request #707 from zax0rz/glm/depth-abilities`)

## Frontier and queue position

The required main checkout/pull and `make fidelity-depth` checkpoint before
this slice reported **602 total cases, 582 proven/delegated, 6 blocked, and 14
excluded**: **582/588 actionable (99.0%)**. The first remaining
un-manifested interpreter command family was `at`, registered immediately
after `abilities` at `src/interpreter.c:323`.

This slice adds nine cases. The post-manifest frontier is **611 total cases,
591 proven/delegated, 6 blocked, and 14 excluded**: **591/597 actionable
(99.0%)**.

## C call path and branch inventory

The C command row is `{ "at", POS_DEAD, do_at, LVL_GRGOD, 0 }` at
`src/interpreter.c:323`. `command_interpreter` performs prefix resolution and
the `LVL_GRGOD` gate before calling `do_at` (`src/interpreter.c:1407-1456`).
The handler is `src/act.wizard.c:242-265`.

`do_at` first calls `half_chop`. Its player-visible branches are:

- no room/name: `You must supply a room number or a name.\r\n`;
- room/name without a nested command: `What do you want to do there?\r\n`;
- plain numeric vnum: `real_room`, with `No room exists with that number.\r\n`
  when absent;
- nonnumeric target: visible character first, then visible object; an object
  not in a room emits `That object is not available.\r\n`, and no target emits
  `No such creature or object around.\r\n`;
- a resolved target temporarily moves the actor, dispatches the nested raw
  command through `command_interpreter`, and restores the original room only
  when the nested command leaves the actor in the temporary room. If the
  nested command moves the actor elsewhere, C leaves that resulting location.

`find_target_room` also contains `ROOM_GODROOM` and occupied
`ROOM_PRIVATE` restrictions for callers below `LVL_GRGOD`; the registered `at`
  surface is itself `LVL_GRGOD`, so those branches are unreachable through
  this command row.

## RED/GREEN proof and confirmed fixes

The new `at-depth` vehicle uses the empty-player Implementor and quiet mobs.
On clean main, seed 1 was RED in the confirmed branches: Go returned the
wrong message for `at 1204`, rejected named character/object targets as
invalid numbers, accepted nonexistent numeric room `999999` as a void, and
therefore did not follow C's `find_target_room` path. The C `--show-oracle`
  block showed the exact usage, missing-command, named-target, missing-target,
  no-room, and nested `where` outputs.

The Go-only fix in `pkg/session/wiz_movement.go`:

- raises the handler-side check to `LVL_GRGOD`;
- mirrors C's distinct no-argument and no-command branches;
- implements C-style leading-digit room parsing and exact no-room text;
- resolves visible characters and objects through the existing canonical world
  resolvers and maps them to their current room;
- removes the invented recursion-depth cap;
- dispatches the nested command with its parsed command/argument boundary; and
- restores the original room conditionally, matching C's post-dispatch check.

`cmd/dp-oracle-diff/scenarios/at-depth.txt` covers:
`at.no-arg`, `at.no-command`, `at.named-character-target`,
`at.named-object-target`, `at.named-target-missing`,
`at.numeric-target-missing`, and `at.execute-and-restore`.
`mortal-batch19.txt` supplies the `at.level-gate` proof. The conditional
non-restoration state branch is pinned by
`TestCmdAtPreservesNestedMovementLocation`; a live nested movement probe was
not used because the nested movement command exposes a pre-existing unrelated
room-observation formatting divergence (`Go` emits an `Exits` line that C does
not).

The seed-1 `--show-oracle` run is GREEN. The same scenario is GREEN on seeds
**2, 3, 5, and 8**. No `src/` or `darkpawns-c-oracle/` file was edited.

## Manifest and gates

`docs/fidelity/depth/at.tsv` records all nine cases as proven: eight
`oracle-green` cases and one `unit-green` state case. `make fidelity-depth`
passes with the frontier stated above.

The full local gates pass:

- `go build ./...`;
- `go vet ./...`;
- `go test ./...`;
- `golangci-lint run ./...` — `0 issues`; and
- `/home/zach/go/bin/gofumpt -l .` — no output.

This handoff follows R1 (exact player-facing bytes), R2 (the registered C
command surface and gate), R4 (removal of invented numeric-only behavior and
recursion cap), R5c (reuse of canonical visibility/target resolution), and R5e
(actual C call-path verification).

## Next queue

After this PR is handled, return to `main`, pull, rerun
`make fidelity-depth`, and take the next un-manifested command family after
`at` in `src/interpreter.c` table order: `advance`.
