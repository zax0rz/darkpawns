# Depth-fidelity handoff — `do_carve` — 2026-08-28

## Frontier and queue position

This session began on `main` at `b54cfa649` after the required pull,
`make fidelity-depth`, and reread of `docs/fidelity/DEPTH_TESTING.md` and the
latest handoff. The frontier was 862 total cases: 840 proven/delegated, 6
blocked, and 16 excluded.

The next distinct un-manifested interpreter-table handler was `do_carve`,
registered as `carve` at `src/interpreter.c:374`, ahead of `do_charge` and
`do_circle`.

## C call-path audit

The source path is `src/interpreter.c:374` → `src/new_cmds.c:115-213`.
`do_carve` calls `one_argument`, resolves a visible room character first, then
checks position, empty input, self/living targets, room object visibility and
keyword predicates. It has no skill gate. A valid object must contain both
`corpse` and `carve_` in its C keyword list, then passes the count-only
`CAN_CARRY_N` gate. C maps exact `carve_meat`, `carve_fish`, `carve_bird`, and
`carve_rabbit` tokens to vnums 8015, 12, 13, and 14, respectively, with 8015
as fallback. It creates food before validating the wielded weapon, accepts only
weapon `value[3]` 3 or 11, emits actor/room `act()` messages, dumps corpse
contents, and extracts the corpse.

The disposable `carve-depth` vehicle uses room 8162, two real vnum-4350
`fish carve_meat` mobs killed through combat, object 17 for the non-carvable
corpse branch, bread for the non-corpse branch, a passive peer, and a real
starter-sword pickup. The oracle and Go port were compared at seeds 1, 2, 3, 5,
and 8; every run ended with no normalized divergence. The vehicle proves the
target-missing, non-corpse, non-carvable-corpse, living-target, self, no-wield,
success/audience/inventory, and trailing-target branches. Focused unit tests
cover fighting-before-no-arg, wrong weapon, full arms, food mappings, corpse
content transfer, and generated NPC corpse keyword preservation.

## Confirmed REDs and Go-only changes

On `main`, carve used an invented `CanUseSkill` gate, checked no-argument before
the fighting branch, had incorrect fighting punctuation, matched only armor
and short-description substrings, omitted the C predicates and carry gate,
created the wrong synthetic food object, omitted room output/content dumping,
and used the wrong weapon and success messages. The C-first fixes are limited
to Go:

- `pkg/command/skill_commands.go` now follows C argument/position ordering and
  removes the invented skill gate.
- `pkg/game/skill_advanced.go` implements the audited character/object gates,
  C food mapping, weapon checks, room audience, content transfer, and corpse
  extraction.
- `pkg/game/death.go` preserves the source NPC keyword list when constructing a
  corpse, matching `fight.c:277-281` and enabling downstream C keyword tests.
- `pkg/game/carve_depth_test.go` and the command test additions provide focused
  state and entry-gate proof.

## Verification and resulting frontier

Manifest: `docs/fidelity/depth/carve.tsv` (15 cases). The disposable vehicle is
`cmd/dp-oracle-diff/scenarios/carve-depth.txt`.

All required gates passed:

- `make fidelity-depth`
- `go build ./...`
- `go vet ./...`
- `go test ./...`
- `golangci-lint run ./...`
- `gofumpt -l .` (clean)

The resulting frontier is 877 total cases: 855 proven/delegated, 6 blocked,
and 16 excluded; actionable completion is 855/861 = 99.3%.

The unrelated untracked `docs/briefs/BRIEF-2026-08-28-economy-specproc-cluster.md`
was preserved. No C oracle files were edited.

Next queue item after this slice: the first remaining un-manifested
interpreter-table handler, `do_charge` (`charge`).
