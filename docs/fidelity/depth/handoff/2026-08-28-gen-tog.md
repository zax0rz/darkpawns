# Depth-fidelity handoff — 2026-08-28 — `do_gen_tog`

## Queue position

- The special-procedure inventory is exhausted, including the single permitted
  `objmagic.sleep-entry-gates` cast-sleep attempt.
- The interpreter-table sweep is complete through the first distinct family
  after `bite`: `do_gen_tog` (`brief`, `compact`, the communication and quest
  preference toggles, `ident`, and `slowns`). The next family must be selected
  from a fresh main checkout, pull, frontier report, and C-table / manifest
  comparison.

## C path verified

- `src/interpreter.c:366-708` registers the `do_gen_tog` command rows. The
  ordinary preference rows are mortal-accessible except for their explicit C
  level/position gates; `ident` and `slowns` are `LVL_IMPL-1` commands.
- `src/act.other.c:1142-1273` returns silently for NPC callers, rejects a
  clanless `noctell` before changing state, rejects an unchosen mortal `nowiz`,
  toggles the PRF bit or process setting, prints the indexed C message, and
  applies `WAIT_STATE(ch, PULSE_VIOLENCE * 2)` only to `nosummon`.
- `ident` toggles the process-wide `ident` setting, initially `NO`; `slowns`
  toggles the process-wide `nameserver_is_slow` setting, initially `YES`.
  `SCMD_AUTOEXIT` exists in the C switch but has no C table row and remains
  owned by the separate `auto` family.

## Confirmed REDs and fixes

- Every existing Go player-preference toggle matched the C on/off bytes in the
  complete Implementor vehicle. The vehicle also proves ignored trailing
  arguments, the clan gate, and the padded two-round `nosummon` cooldown.
- The C table exposed two real command-surface gaps: Go had no `ident` or
  `slowns` registrations, so both returned `Huh?!?` through command-gate
  handling while C emitted their toggle messages. Go now registers both with
  the C-derived level-39/dead-position gates and implements their exact global
  state/message transitions. No files under `src/` or
  `darkpawns-c-oracle/` were changed.

## Proof and gates

- Before the fix, `gen-tog-depth --show-oracle --seed 1` showed the four RED
  blocks for `ident` and `slowns`; all other blocks were already green.
- After the fix, the same vehicle is green and covers all 18 C table toggles,
  both directions, ignored arguments, and the required `~dpclock pulse 20`
  padding after `nosummon`.
- Focused tests cover every PRF mapping, the clan and direct `nowiz` gates,
  the global ident/nameserver toggles, the command registrations/gates, and
  the exact `nosummon` wait state.
- Full local gates pass: `make fidelity-depth` (830 total, 809
  proven/delegated, 6 blocked, 15 excluded; 99.3% actionable),
  `go build ./...`, `go vet ./...`, `go test ./...`,
  `golangci-lint run ./...`, and `make check-fmt`.
- PR #727 (`glm/depth-gen-tog`) passed hosted lint, security, and test checks;
  build and deploy were skipped by CI. It was merged as `4043b6bb3`.

## Carry-forward

- Preserve the unrelated untracked brief
  `docs/briefs/BRIEF-2026-08-28-economy-specproc-cluster.md`.
- Return to `main`, pull, rerun `make fidelity-depth`, reread
  `docs/fidelity/DEPTH_TESTING.md` and this newest handoff, then take the next
  un-manifested interpreter-table family in order.
