# Dated Handoff: 2026-08-27 (god-flag/condition vehicle round)

- The God-warmup vehicle class is live: empty-players crowns the PRIMARY
  Implementor, and drained `[warmup]` commands on that God now cover two
  state-control surfaces — `mute <mortal>` (do_wizutil SCMD_SQUELCH toggles
  PLR_NOSHOUT) and `set <mortal> drunk|hunger|thirst <n|off>` (do_set cases
  29-31, RANGE(0,48), "off" → -1). Go cmdSet gained the three condition
  fields with C's ack bytes (unit: TestCmdSetConditions).
- Six comm.tsv rows converted oracle-green: say.noshout, tell.noshout,
  whisper.noshout, emote.noshout (comm-noshout), tell.notell-self and
  tell.cant-hear (comm-notell — mortal `notell` toggle, no God needed),
  say.drunk-speech (comm-drunk). emote.noshout was RED pre-fix: Go cmdEmote
  had no PLR_NOSHOUT/INT gate (R1/R4).
- say.drunk-speech exposed a REAL hidden divergence (R5c): Go's speakDrunk
  used longest-match-per-position substitution, but C's speak_drunk loop has
  NO break — a match advances the cursor and the table scan CONTINUES from
  the next entry, so "kill" can never fire after "the " matched in the same
  pass ("the killer is here" → C "th' killer ish here", Go was
  "th' murderizeer ish here"). applySyllableSubstitution now replicates the
  C scan (byte-oriented, one unmatched byte per pass) and drunkSyllables is
  the exact C table — the single-letter IDENTITY entries are load-bearing:
  they consume letters mid-scan ("Sick" → "SHick", not "SHishkin").
- Same latent algorithm bug remains in the RACE tables: Go's dwarf/elf/gnoll/
  draconian/giantish/deadspeak tables are reduced hybrids (dwarf: 32 entries
  vs C speak_dwarven's 82 incl. the full-letter cipher) and C also has
  kender/minotaur/ssaur/human tables with no Go counterpart. do_race_say
  needs its own round with exact tables + the new scan.
- say.stupid-gate still blocked, but the note is updated: the `set` vehicle
  exists, blocked on cmdSet stat-range parity (C mortal 0-18 vs Go clamp
  3-25) — a do_set field-table round would unblock it.
- tell.linkless / tell.writing / tell.npc-nodesc remain blocked (need a
  dropped connection, the board-writing surface, and a charmed NPC).
