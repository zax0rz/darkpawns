# Deployment Runbook

The canonical current production runbook is [`../../DEPLOYMENT.md`](../../DEPLOYMENT.md).

This file intentionally contains no duplicate commands. Production topology,
paths, and rollback steps drifted when two deployment guides were maintained.
Update the root runbook instead.
