# Session Notes — 2026-06-27

## Summary

Two security/cleanup batches completed and deployed before 8:30 AM. 10 Reek findings resolved, one class-level audit completed.

## Batch 1: Security Hardening (6 issues)

**Branch:** `fix/security-hardening-20260627` (Kimi implemented, Daeron verified)
**Commits:** 66ecc389, 5cdd2b44, 3876b8b0, f459b053, aeb367c8, 55aa068c
**Status:** Merged to main, deployed to CT 120

| Issue | Severity | Fix |
|-------|----------|-----|
| DP-591 | HIGH | Hardcoded Postgres credentials removed |
| DP-596 | HIGH | WebSocket origin bypass removed |
| DP-557 | HIGH | Reverse-DNS ban checks restored |
| DP-595 | MEDIUM | pprof bound to localhost |
| DP-592 | MEDIUM | Account-level lockout added (10 fails / 15 min) |
| DP-550 | MEDIUM | k8s JWT_SECRET + ENCRYPTION_KEY from dp-secrets |

## Batch 2: Validation Cleanup + File Permissions (4 issues + sweep)

**Commit:** ac5ed39b + 3a85c5c5 (Reek implemented, Daeron fixed brace alignment)
**Status:** Merged to main, deployed to CT 120

| Issue | Severity | Fix |
|-------|----------|-----|
| DP-570 | MEDIUM | SanitizePlayerName min length contract fix |
| DP-569 | MEDIUM | SanitizeInput truncate-before-escape |
| DP-597 | LOW | Agent store 0o644 → 0o600 + MkdirAll error logging |
| DP-573 | LOW | Validation cleanup: tests, Makefile, deprecation comment |

**File permissions sweep:** 7 additional 0o644 → 0o600 across agentcli (state, events, client), game/clans, dreaming (graph, summary, result).

## Key Observations

- The pipeline (Reek → Daeron brief → Reek implement → Daeron verify → Architect merge) is working well
- Daeron's audit of 0o644 sites after DP-597 found 7 more sensitive files — "find one, find the class" pattern
- Account lockout implementation (DP-592) was the most complex — had concerns about DB write path, but Kimi indexed the column and used atomic UPDATE
- False positive on DP-573 (stale file references) — Profiler struct only has `cpuProfile`, other functions use local variables

## Commits Deployed

- `55aa068c` through `66ecc389` (security batch)
- `ac5ed39b` (validation + permissions)
- `3a85c5c5` (brace alignment fix)

## Open Items

- DP-598 (MCCP2 telnet compression) — deferred, medium complexity
- DP-556 (per-site connection counting) — deferred, fidelity concern
- DP-594 (WebSocket private IP trust) — low, needs agent-key auth design
- DP-599 (go-sqlite3 patch update) — trivial, can batch next time
