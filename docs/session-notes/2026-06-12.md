# Session Notes — 2026-06-12

## Summary

Massive session. Board sweep, 9 issues closed, CI fixes, workflow established.

## What Happened

### Board Sweep
- Audited all open Linear issues (50+ across CRITICAL/HIGH/MEDIUM/LOW/BACKLOG)
- Prioritized: security, correctness, data loss bugs first
- Closed duplicate DP-581 (duplicate of DP-574)

### Batch 1: Critical Fixes (Gemini 3.5 Flash)
- **DP-527 (CRITICAL):** Tick() dropped expired affect cleanup — Phase 2 called RemoveAffectsBySpell which re-acquired lock and iterated list that Phase 1 already emptied. Fixed by calling removeAffectImmediate/ClearStatusFlag directly in Phase 2.
- **DP-561 (HIGH):** Shop.Restock() deadlocked — RWMutex re-entrant. CanSellType() called under Lock() tried to RLock(). Fixed by inlining the type check.
- **DP-558 (CRITICAL):** Go 1.26.3 → 1.26.4 (stdlib vulns)
- **DP-559 (HIGH):** golang-jwt CVE-2025-30204
- **DP-560 (MEDIUM):** x/crypto v0.51.0 → latest (14 SSH vulns)

### Batch 2: Ban System (Gemini 3.5 Flash)
- **DP-553 (HIGH):** Wildcard ban matching — `strings.Contains` doesn't handle `*`. Added `path.Match()` (OS-independent, not `filepath.Match`).
- **DP-554 (HIGH):** ValidName missing online duplicate check — HasActiveCharacter callback wired but never called. Added case-insensitive check with `strings.EqualFold`.
- Bonus: session_login.go moved ValidName check to new character creation only (returning players with password skip profanity filter).

### Batch 3: Server Safety (Gemini 3.5 Flash)
- **DP-562 (HIGH):** Door race condition — DoorManager action methods held no lock during mutation. Fixed by holding dm.mu.Lock() for entire operation.
- **DP-566 (HIGH):** os.Exit in server goroutine — replaced with errChan + select pattern for graceful shutdown.

### CI Fixes
- go-version 1.26.3 → 1.26.4 in ci.yml (matched go.mod upgrade)
- golangci-lint-action v7 → v8 (Node 24 deprecation warning)

### Branch Cleanup
- Deleted 28 stale remote branches (only `main` remains)
- Deleted 9 stale local branches

### Workflow Established
- Brief template updated with PR flow, regression test guidance
- `docs/briefs/README.md` is the operational playbook
- Flow: Daeron writes brief → coding agent branches & implements → Daeron reviews PR → merge → Linear update

### Infrastructure
- Created DP-590: Migrate Dark Pawns from Docker to Proxmox LXC container
- Investigated 108k "max in world" mob spam — zone reset noise, not actual duplication. Recommend downgrading log level.
- Server running on Docker since May 27, 0 restarts, healthy

### Discovered
- darkpawns.net — parallel revival run by Vintage, using Frontline's C code. Different mission (live game vs archival). Zero conflict.
- The Architect told them about the Go port on June 7th. Egon tried to connect June 8th and failed (WebSocket/TLS issues likely).

## Open Items
- DP-557 (DNS dead code) — next brief
- DP-562/DP-566 — server safety brief already written at `docs/briefs/BRIEF-2026-06-12-server-safety.md`
- Log persistence problem — Docker stdout only, no disk persistence. Needs volume mount or Proxmox migration (DP-590).
- 108k mob spam — downgrade "max in world" from WARN to DEBUG
- gbrain issue flagged by Blenda

## Commits This Session
- `7c3152d` — fix: affect cleanup, shop deadlock, Go upgrade (DP-527, DP-561, DP-558, DP-559, DP-560)
- `5eb2e54` — fix: CI go-version 1.26.3 → 1.26.4
- `548b472` — fix: door race condition + graceful shutdown (DP-562, DP-566)
- `75c0776` — chore: golangci-lint-action v7 → v8
