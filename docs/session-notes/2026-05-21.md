# Session Notes — 2026-05-21 (evening continuation)

## Session 57 — DP-Goat: Protocol Layer + Architecture Design

### What Happened

Started with BRENDA (MiniMax M2.7) play testing Dark Pawns — she successfully completed character creation as Brenda69, a dwarven psionic at the Temple Altar (room 8004). SEEP worked, but the harness reconnection loop (Mode B failure) prevented sustained play.

This led to a broader architectural discussion about how to give agents persistent connections to a MUD. Zach identified the Printing Press project (printingpress.dev) as a model for agent-native CLIs. Blenda contributed deep research on AEROS, SSH ControlMaster, WebSocket reconnection protocols, and behavior tree subsumption layers.

**Key insight:** The "agent within an agent" pattern — a persistent daemon body vs an ephemeral LLM mind. The daemon holds the WebSocket 24/7. The mind connects, plays, disconnects. The daemon buffers events and generates context packets for the mind when it wakes.

**DP-Goat spec** was drafted by Blenda and landed at `/Users/zach/.openclaw/workspace/darkpawns/SPEC-DP-GOAT.md`. Covers full architecture, 40+ CLI commands, 4-phase plan, and 7 open questions.

### Linear Project

Blenda created the DP-Goat project (💜) with 18 issues recreated from Daeron's original set. All P0 items marked Done.

### Server-side Changes (all deployed to frankendell)

**P0-1 (DP-245):** Sequence numbers on all outbound messages — `e63d145`
**P0-2 (DP-246):** ANSI suppression for agent sessions — `9c921f1`
**P0-3 (DP-247):** Session handoff grace period (5s probe) — `1356c7d`
**P0-4 (DP-248):** Expanded state variable subscriptions (MOVE, MAX_MOVE, GOLD, POSITION) — `bc965a9`

**Also:** Fixed duplicate char name leak in `completeCharCreation` (977f78d), fixed CI to not fail on skipped Docker push/deploy (e69e196).

### Key Observations

- **The reconnection problem is architectural, not a bug.** Agents need a persistent daemon, not better scripts.
- **SEEP helped Brenda** — she got through creation. The post-creation connection drop is a harness timeout issue.
- **Printing Press model** maps perfectly to DP's needs. A `dp-goat` CLI would solve the "every agent writes their own script" problem.
- **Zach noted his ADHD pattern** — rapid extrapolation from one bug to full system overhaul. I called it out, he recognized it, and chose to enter the spiral deliberately. Good.

### What's Open

- P1 (daemon implementation) is the next phase — state model, WebSocket persistence, Unix socket listener
- P0-4 server-side is done; no client-side code started
- BRENDA still can't maintain a sustained session — dp-goat will solve this
- CI now skips gracefully instead of failing

### Research Log Entry

Added `[DIGEST] 2026-05-21 — DP-Goat architecture, SEEP deployed, 4 P0 items shipped` — the daemon architecture is the most paper-relevant development of the session.
