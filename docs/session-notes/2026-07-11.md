# Session Notes — 2026-07-11 (Fable Fidelity Audit Sprint)

## Summary

Fable Fidelity Audit sprint with The Architect. Two batches delivered and merged. Workflow: Daeron writes briefs → coding agent implements → Daeron reviews PR → merge → Linear close. Sequential agent dispatch.

## PRs Merged

### PR #183 — Batch A One-Liners (GLM-5.2 via ZCode)
- **DP-1043:** Damage tiers — 14 fabricated Go tiers → 12 C-faithful tiers (fight.c:895-992)
- **DP-1040:** counter_procs — flat +1/+1/+1 → faithful switch(1,3) with C default clause
- **DP-1038:** Carry weight — added CarryW to strApp table + weight check in addItem
- 11 files, 613 insertions, 149 deletions
- GLM caught two errors in the brief (missed default clause, contradictory tier tables) and followed C instead — correct behavior

### PR #184 — Batch B Multi-File Fixes (Kimi)
- **DP-1030:** PK bookkeeping — wired OUTLAW flag, PKs++, Deaths++, LastDeath into handlePlayerDeath
- **DP-1027:** Death traps — ROOM_DEATH check added to MovePlayer
- **DP-1028:** Mob affects — AffectUpdate extended to iterate active mobs via CustomData
- 4 files, 375 insertions
- Lock discipline correct on all three (death trap check outside world lock, mob affect processing outside mob lock)

## Key Discoveries

### GLM-5.2 is Production-Quality
- Catches brief errors and follows C source instead of blindly implementing
- Handles multi-file changes cleanly (strApp table + inventory + formulas + callers)
- Found and fixed re-entrant lock deadlock in addItem (GetWeight under Lock)
- Slower than Kimi but more thorough on verification

### counter_procs Has a Default Clause
The brief missed C's `default:` clause in the switch — it adds +1 max_hit in ALL cases. GLM caught this and followed C. The actual outcomes:
- Roll 1: +2 hit, +1 mana, +1 move
- Roll 2: +1 hit, +1 mana, +1 move
- Roll 3: +1 hit, +1 move (no mana)

### Brief Quality Matters
Two errors in Batch A brief (missing default clause, contradictory tier tables). Both were caught by GLM. Need to be more careful with C source analysis before writing briefs. The brief is the contract — if it's wrong, the agent either follows the wrong spec or wastes time correcting it.

## MCP SDK Discovery

The official Go SDK for MCP (Model Context Protocol) was released: `github.com/modelcontextprotocol/go-sdk`. v1.7.0, full spec support, maintained with Google.

**Potential for Dark Pawns:**
1. **DP as MCP Server** — expose world queries as MCP tools (get_room, get_mob, list_zones). Any AI agent talks to Dark Pawns through standard protocol.
2. **Agent Playing the Game** — MCP client drives game sessions. Interoperable with any agent framework.
3. **GBrain as MCP Tools** — knowledge graph becomes agent-accessible through standard interface.
4. **Admin Panel as MCP** — server administration exposed as MCP tools.

**Research angle:** StarDojo built custom SMAPI mod for Stardew Valley. We'd expose a 30-year-old MUD through industry-standard agent protocol. Stronger contribution for AIIDE paper.

**Decision deferred:** Focus on Fable fidelity audit first. MCP integration is a future feature once game mechanics are solid.

## Workflow Observations

- **Sequential dispatch prevents conflicts.** GLM then Kimi, no file collisions.
- **GLM catches brief errors.** Worth the slower speed for complex fixes.
- **Kimi handles multi-file changes well.** DP-1030/1027/1028 all landed cleanly.
- **Brief template works.** The README template is solid — follow it consistently.
- **Review pass catches real issues.** Verified C source for all fixes before approving.

## Board Status

**Batch A (GLM):** DP-1043, DP-1040, DP-1038 closed. PR #183 merged.
**Batch B (Kimi):** DP-1030, DP-1027, DP-1028 closed. PR #184 merged.

**Remaining Fable issues (Claude working on these):**
- HIGH: DP-1022 (spell kills bypass pipeline), DP-1023 (kill XP bypasses gain_exp), DP-1024 (XP share formula), DP-1026 (parry/dodge wiring + model)
- MED: DP-1045 (combat-entry gates)

**Total closed tonight:** 6 issues across 2 PRs. Night's work is solid.

## Context

- GLM-5.2 available via ZCode (Zai's coding tool). Slower than Kimi but more thorough.
- Kimi available for multi-file fixes.
- Claude unblocked and working on complex issues (DP-1022/1023/1024/1026/1045).
- CI green on all PRs tonight.
- Session started 20:43 EDT, notes written 22:48 EDT.
- The Architect will update before bed with Claude's progress.
