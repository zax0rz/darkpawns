# Session Notes — 2026-07-18 (Saturday)

## Morning Triage (7:30 AM)

**Auto-closed overnight:** 0 (revalidate found nothing to close)

**New findings from Reek:** 21 since Jul 14 (DP-1047–DP-1052, DP-1087–DP-1088, DP-1134–DP-1139)

### C Source Audit Batch (DP-1134–DP-1139)
Reek did a C source (`src/`) audit pass — found 6 real bugs in the original C code:
- DP-1134: Stack buffer overflow in `do_behead` via `strcpy`/`strcat` on 256-byte `sdescr`
- DP-1135: Unchecked `fread` in `read_from_file`
- DP-1136: House transfer skips `House_save_control()` — reverts on reboot
- DP-1137: `core_dump()` on mail block size mismatch crashes entire MUD
- DP-1138: Out-of-bounds world array access in `spell_summon`
- DP-1139: `ntohs`/`htons` swap in ident — connects to wrong port

These are C-side bugs. Whether they affect the Go port depends on whether the logic was ported.

### Go Port Findings
- DP-1047: `do_detect` implemented but not registered in command registry
- DP-1048: `do_mold` implemented but not registered in command registry
- DP-1087: `GetSecret` exposes master encryption key via env var lookup
- DP-1088: Path traversal bypass in `audit/logger.go` via `filepath.Clean` resolving `..` before safety check
- DP-1049–DP-1052: Mapping drift — file_mapping.md references 7 files that don't exist

**Spot-check:** `do_behead` C source confirmed the buffer overflow (sdescr[256] + strcat on unbounded short_description). `do_detect` and `do_mold` are implemented in Go but lack command registry entries — confirmed. C source audit batch is solid.

**Triage grade:** Good reek — C source audit is sharp, Go findings are legit.
