# Session Notes — 2026-05-27 (Session 77)

## What Happened

1. **MiMo API price reduction** — Fuli Luo announced up to 99% cut on cache hit pricing. 1:7 Full:SWA sparsity ratio is the architectural reason. Good for our inference costs.

2. **dp-client security fixes** — All 3 remaining blockers from the May 12 review fixed in one session using CodeWhale + DeepSeek Flash:
   - DP-518: Path traversal via character name (filepath.Rel defense-in-depth)
   - DP-519: wss:// support (default to wss, --insecure flag for local dev)
   - DP-520: Password logging in ParseCommand (one-line fix)
   - DP-517 was already fixed in code (Lua sandbox)

3. **Unified Client Platform vision** — The Architect asked "could we do all of this in just one platform?" Answer: one core library, two rendering layers. Created DP-516 with full roadmap (7 phases, security → core → GMCP → sidebar → cross-machine → remote commands → polish).

4. **Gemini deep research** — Full report on multi-character TUI design. Saved to `research/multi-character-mud-tui-design.md`. Key insight: GMCP support on the server is the prerequisite for structured party data.

5. **CodeWhale evaluation** — First use of Hmbown/CodeWhale with DeepSeek Flash. Worked perfectly for surgical fixes. Proven workflow: write brief → hand to CodeWhale → Flash executes → tests pass.

## Decisions Made

- **One core library, two apps** — dp-client (TUI) and dp-agent (headless) share `pkg/client/core`. Not a monolith.
- **Phase 0 first** — Security fixes before features. Done.
- **Flash over Pro for briefs** — Pro is overkill for one-line changes. Save for complex refactors.
- **GMCP is necessary** — Party vitals sidebar can't work reliably on MSDP/text parsing alone. Server needs GMCP support (DP-523).

## What's Open

- Phase 1 (core library refactor) ready for DP-521 brief
- Phase 2 (GMCP support) can run in parallel — server-side work
- 10 fidelity briefs from session 76 still need execution
- Daeron immortal vision doc written but not actionable yet
- The Architect wants a cheap Apple laptop for couch-based management

## Commits This Session

None by Daeron. Security fixes committed by CodeWhale/DeepSeek in dp-client repo.

## Board Status

- DP-517: Done (Lua sandbox — already fixed)
- DP-518: Done (Path traversal)
- DP-519: Done (wss:// support)
- DP-520: Done (Password logging)
- DP-516: Open (Unified Client Platform — parent issue)
- DP-521 through DP-526: Open (Phase 1-6 roadmap)
