# Session Notes — 2026-07-06

## Morning Triage (7:30 AM ET)

**Reek's overnight fidelity review:** 11 Linear issues (DP-996 through DP-1006) posted to Discord at 08:03 UTC. Clawpatch report file not found locally (`.clawpatch/reports/20260706T063127-79d782.md` missing).

**Triage outcomes:** 4 confirmed, 6 rejected, 1 needs context. 55% false positive rate on the game-mechanic subset.

### Confirmed
- **DP-1000** (MEDIUM) — L command Arg3 semantic mismatch. zon.go vs parser.go disagree on what Arg3 means. C convention is key_vnum; parser.go is correct, zon.go comments are wrong.
- **DP-1001** (HIGH) — Duration-0 affect semantics. Internal inconsistency: `affect.go` says Duration==0 is permanent, `affect_update.go` says it expires this tick. Update loop matches C; Affect methods don't.
- **DP-1002** (LOW) — Animate Dead consumes corpse before verifying summon. Same as C source. Consistent but still a design issue.
- **DP-1005** (MEDIUM) — Mutex held during Lua DoFile/PCall. `RunScript()` locks the entire engine for the duration of script execution.

### Rejected
- **DP-996** — Shopkeeper combat pairs. StopCombat() already removes pairs via delete(). Code handles this correctly.
- **DP-997** — LoadShops merges state. LoadShops clears maps before inserting. Replaces, doesn't merge.
- **DP-998** — SaveShops omits fields. All fields (Gold, BankAccount, WithWho, Inventory) are saved and restored.
- **DP-999** — Movement sector bounds panic. Bounds check exists at act_movement.go:243-246. Reek missed it.
- **DP-1003** — Door operations one-side. DoorManager mirrors via `mirrorToReverseDoor()`.
- **DP-1004** — Concurrent sells overdraw. TryDeductGold is atomic under mutex.

### Needs Context
- **DP-1006** — Shared Lua state stale triggers. Plausible but needs concrete reproduction path.

### Server Status
- Running, 104MB RSS, 0% CPU
- Zone 15100 max_in_world WARN spam (cosmetic)
- ~12 mobs panic-looping every tick (known, recover() catches them)

### Actions Taken
- Posted triage summary to #dark-pawns
- Added triage comments to all 11 Linear issues
- Moved 6 rejected issues to Canceled
- Moved 4 confirmed issues to In Progress with severity/priority labels
- Left DP-1006 open with NEEDS CONTEXT comment

### Observations
- Reek's false positive rate is high on this batch (55%). The rejected findings were all cases where the code already had the fix — Reek didn't check whether the fix existed before filing. This is a pattern: Reek identifies the vulnerability class but doesn't verify the current state of the code.
- The confirmed findings are real — DP-1001 (duration-0 semantics) is the most impactful because it's an internal inconsistency that could silently break affect behavior.
