# Session Notes — 2026-07-04 (Evening)

## What Happened

Triage + Tier 0 CI clearance + stale-finding sweep. User asked for backlog
triage and to start picking away at it. 208 open issues → triaged into tiers.

## PRs Merged

### PR #50 — CI blockers DP-922 + DP-929 (squash-merged)
- DP-922: Flaky `TestDoCharge_*` RNG tests. Routed `DoCharge` success roll
  through existing `combat.Roller` abstraction (already used by pkg/combat
  tests); wrapped flaky tests in `combat.WithRoller(NewScriptedRoller(...))`.
  Production behavior unchanged. Verified 20/20 `-race` runs.
- DP-929: U1000 ratchet test silently found 0 files. Replaced
  `filepath.WalkDir` (broken in macOS test sandboxes) with recursive
  `os.ReadDir`. Added `TestWalkFindsRepoGoFiles` regression guard.

## Issues Closed (13 total)

**Shipped fixes (2):** DP-922, DP-929

**Sweep — already-fixed duplicates (11):**
- DP-747 (shop persistence) — dup of DP-925
- DP-750 (shopkeeper combat pairs) — dup of DP-923
- DP-792 (movement panic invalid sector) — dup of DP-924
- DP-741 (duration-0 affects) — resolved by DP-669 design decision
- DP-743 (area/mass spells wrong charm flag 1<<2 vs 1<<10) — fixed
- DP-727 (animate dead consumes corpse early) — fixed
- DP-761 (psionic spell speech class IDs 5/7 vs 9/11) — fixed
- DP-762 (spell speech substitution/delivery) — fixed
- DP-744 (admin login panic on missing player) — fixed
- DP-716 (JWT accepts any HMAC algorithm) — fixed
- DP-777 (backfire chance inverted for Disintegrate/Disrupt/Psiblast) — fixed

## Issues Filed (1)
- DP-930: CI lint broken repo-wide. `golangci-lint v1.64.8` (built with
  go1.24) cannot load `go.mod`'s `go 1.26.4`. Affects every PR — last 3
  commits on main all failed `lint` the same way. Local lint (v2.12.2)
  reports 0 issues. Bump the action pin.

## Sweep Methodology

Dispatched a general-purpose subagent to read each open Reek issue's full
description via `mcp__linear__get_issue`, examine the cited `file:line`, and
classify as ALREADY-FIXED / STILL-OPEN / STALE-DUPLICATE / NEEDS-VERDICT.
Every verdict backed by code citation.

Per AGENTS.md ("subagents have lied before"), spot-checked 3/11 verdicts
directly before bulk-closing:
- DP-743: confirmed `engine.AFFCharm` not literal `1<<2`
- DP-727: confirmed spawn-before-extract order
- DP-716: confirmed `WithValidMethods([]string{HS256})`

All 3 confirmed accurate. Sweep agent was reliable.

## Sweep Findings — Genuinely Open (left alone)

- **DP-760** — raw `$N` act token sent to player at `act_social.go:94`
- **DP-795** — JWT issuer not enforced during validation (`jwt.go:119-126`)
- **DP-826** — `lvlGod=34` hardcoded; values currently match `LVL_GOD=34`
  so no live defect, future-drift only. Needs human verdict.
- DP-192, DP-190, DP-191 — fidelity/restoration tracking, not stale.

## Board State

- 0 CRITICAL open
- 0 HIGH bugs open (game logic)
- 1 HIGH infra open: DP-930 (CI lint)
- Tier 0 (build-gate blockers) cleared
- Sweep shrank backlog by 13 issues, verified each close

## Context for Next Session

- **DP-930** is the next CI hygiene item — bump golangci-lint-action to a
  v2.x release built with go1.26+. Small workflow edit.
- The remaining ~140 Reek tickets are real defects (concurrency, security,
  test coverage) — not stale duplicates. Next batch should pick a theme:
  - Security: DP-715/DP-718 (hardcoded creds), DP-795 (JWT issuer),
    DP-855 (router doesn't validate tokens)
  - Concurrency: DP-702/DP-734 (scripting races), DP-724/DP-745 (event bus),
    DP-775 (shop deadlock)
  - Test coverage: DP-658/666 (command dispatch, game) — big lift
- Pre-existing CI lint failure means all PRs show red on `lint`. Local
  `golangci-lint` is the source of truth until DP-930 lands.

## Triage Tier Reference

- **Tier 0** (CI blockers): ✅ cleared
- **Tier 1** (real prod bugs): DP-893 (/api/health 401), DP-601 (DB ownership),
  DP-536/DP-529 (type safety), DP-671 (~200 stale Clawpatch findings)
- **Tier 2** (Reek security/concurrency): DP-716 closed; DP-795, DP-702,
  DP-734, DP-715, DP-718, DP-744 closed, DP-757/DP-775 open
- **Tier 3** (stale duplicates): ✅ swept (11 closed)
- **Tier 4** (parked features): unchanged
