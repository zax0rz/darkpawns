# Session 44 — 2026-05-15

## What Happened

Evening session with The Architect. Two rounds of fixes.

### Round 1: DP-159 (MobInstance Mutex Compliance)

The last CRITICAL data race. MobInstance fields (RoomVNum, Fighting, CurrentHP, etc.) were accessed directly from multiple goroutines without mutex protection — same pattern as DP-143 Player fields.

**Added 18 new mutex-protected getters/setters to MobInstance:**
- GetTarget/SetTarget, GetMemory, GetMountRider/SetMountRider
- GetHuntingID/SetHuntingID, GetAffects/SetAffectFlags
- HasAffect/ClearAffect, GetMobFlags/ClearMobFlag
- IsFighting/GetFightingTarget, GetMaxHP/SetMaxHP

**Replaced ~60 direct field accesses across 12 files:**
- spec_procs.go (me), spec_procs2.go (mob), spec_procs3.go (me), spec_procs4.go (me, m)
- limits_condition.go (m), ai.go (mob), follow.go (mob), graph.go (m)
- mobprogs.go (m), scripts.go (m), spawner.go (mob)

save.go and deferred_fight_fns.go retain direct field access under existing locks — correct pattern, would deadlock if changed to getters.

4 subagents used (2x DeepSeek V4 Flash, 2x GLM-5.1). Build, vet, tests all green.

Commit: `c8223ab`

### Round 2: Triple Kill (DP-127, DP-137, DP-147)

**DP-147 (doAmbush sector check):** Cancelled as false positive. Go code `sector != 3 && sector != 4 && sector != 5 && sector != 1` matches C source exactly. Both allow ambush in forest(3), hills(4), mountain(5), city(1). Reek misread the negation logic.

**DP-127 (exp table):** Ported C source `find_exp()` from class.c:1089-1183. Class-specific modifiers (warrior 0.7, thief 0.1, paladin 1.9, etc.) with hardcoded levels 0-12 and formula for 13+. Go was using `1000 * level` — 45x off at level 10.

**DP-137 (combat_basic mob death):** doDisembowelMob now checks `m.GetHP() <= 0` after TakeDamage and calls `handleMobDeath` instead of always calling `startCombatBetween`. Removed the TODO comment. Broader pipeline issues (sanctuary, alignment, wimpy) noted as separate concerns.

Commit: `c85860f`

### Round 3: Simplified Board Cleanup

Cancelled 5 rejected/skipped issues that were still in Backlog:
- DP-117 (DoDig) — rejected, Go is new custom functionality
- DP-118 (DoSerpentKick) — rejected, C is also single-target
- DP-126 (comm_channel) — rejected, Go matches C
- DP-129 (mail) — rejected, C also takes single recipient
- DP-130 (affect_helpers) — skipped, documentation-only

Note: Linear uses "Canceled" (one L), not "Cancelled" (two L's).

## Board State After Session

- **In Progress:** 0
- **Backlog (bugs):** 1 — DP-155 (dual affect systems, architectural)
- **Backlog (features/research):** ~20 (admin panel, website, TUI, research)
- **Simplified:** 0 open (all 21 resolved)
- **Total resolved:** 160

## Operational Notes

- Xiaomi annual plan at 85% (609M/720M tokens). May need to bump tier.
- The Architect called it a night after this session.
