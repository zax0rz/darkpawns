# Session 41 — 2026-05-15

## What happened

### Morning Triage
- Triage'd 8 Reek findings from the overnight crawl
- 2 rejected (DP-104: AffWaterwalk shared type — faithful C port; DP-101: PosStanding — faithful C port)
- 6 confirmed (DP-102, DP-103, DP-105-108)
- Both rejects were "Reek didn't check C source" — prompted workflow changes

### AGENTS.md Updates (mine)
- Updated Program 1 to auto-create Linear issues during triage (was manual gap)
- Added C source citation as mandatory, "Simplified" as dirty word
- Added port fidelity section with search patterns for silent simplifications

### Reek AGENTS.md Updates
- Added C SOURCE VERIFICATION (mandatory for game mechanics findings)
- Added SILENT SIMPLIFICATION DETECTOR (search patterns, reporting format)
- Fixed day-of-week schedule: added pkg/admin (Sunday), pkg/dreaming (Saturday), pkg/agentcli (Wednesday)
- Fixed stale refs: findings-tracker.md → Linear, DARKPAWNS.md removed, pkg/spell/ → pkg/spells/, pkg/mob/ → pkg/game/
- Fixed numbering (duplicate step 3)

### Quick Wins (batch commit `6d345df`)
- DP-105: router.go merge conditional assignment
- DP-106: log_buffer.go WriteString+Sprintf → Fprintf
- DP-107: dp-agent/main.go client.Close explicit discard (2 locations)
- DP-108: handlers_test.go nested if → switch
- DP-102: log_buffer.go buf.Write() error checking
- DP-72: Reek auto-create Linear issues (done today)

### DP-103 — luaSteal (commit `2b9d232` then `439281f`)
- Initial fix: documented broken sentinel (vnum=0 doesn't mean "any item")
- The Architect pushed back: "steal is broken, we should fix that"
- Discovered C lua_steal takes steal(victim, obj) — specific object reference
- Go port was wrong 3 ways: wrong args, no inventory exposure, items went to room
- Dispatched GLM-5.1 subagent for full implementation
- Subagent timed out but landed most of the work; I finished the compilation fix
- Final implementation: objToTable with obj_id, luaSteal rewritten, RemoveObjByInstanceID, GiveItemToMob, Player.GetInventoryItems, ObjectInstance.GetInstanceID
- 892 insertions across 18 files

### DARKPAWNS.md Created
- Master strategy document at repo root
- Self-contained overview for any model picking up the project cold
- Covers: world stats, codebase structure, agent system, research framing, infrastructure, ground truth rule, current status
- Was planned but never created (Reek referenced it, file didn't exist)

### Simplification Audit
- Scanned codebase for "Simplified" comments: 38 found
- Created 3 Linear issues for top offenders:
  - DP-109 (MED): item-use routing no-op
  - DP-110 (HIGH): str_app hardcoded 50lb
  - DP-111 (HIGH): CAN_SEE awake-only check

## Key insight
The steal bug wasn't a mistake — it was a simplification someone made during the port and never flagged. 38 similar simplifications exist. The port will never be "finished" but every one we surface is a data point for the paper.

## Commits
- `6d345df` — Reek findings fixes + DARKPAWNS.md
- `0c92850` — DP-103 initial (documented sentinel)
- `2b9d232` — DP-103 StealRandomItemFromChar (superseded)
- `439281f` — DP-103 full luaSteal implementation

## Board status
- All 8 Reek findings from this batch: DONE (2 rejected, 6 fixed)
- DP-72: DONE (Reek → Linear wiring)
- New: DP-109, DP-110, DP-111 (simplification findings)
- 38 total "Simplified" comments — 4 now tracked, 34 remaining

## What's open
- 34 "Simplified" comments untracked
- Admin Panel Phase 6 (5 issues in backlog)
- Website v2 (3 issues in backlog)
- Research/paper writing (3 issues in backlog)
- TUI Setup Wizard, dp-client integration testing

---

# Session 42 — 2026-05-15 (afternoon/evening)

## What happened

### VPS Research
- Frankendell went down, took MUD server + website offline
- Researched VPS options: Hetzner CX23 (~$4.50/mo), IONOS ($2/mo), DigitalOcean ($4/mo), LightNode Taiwan (~$7.70/mo)
- Architect considering DP + Hardware Hunter + Chad on one VPS
- Site code safe on GitHub — `darkpawns-site/` Hugo repo, `dp-client/` Go TUI, `darkpawns_repo/` server + admin-ui
- Website design spec: `WEBSITE-MASTER-PLAN.md` (620 lines, Claude Design brief)

### Simplification Sprint
- Scanned codebase: 39 "Simplified" comments, 12 TODOs, 20+ Lua TODOs
- Created 26 Linear issues from the scan
- 3 waves of subagents + cleanup + single fix = 11 total agents
- Providers: DeepSeek V4 Flash, GLM-5-turbo, Kimi K2.6 (rotated to avoid rate limits)

**Results: 27 issues resolved (25 fixed, 2 rejected)**

Fixed: DP-109 (item-use), DP-110 (str_app), DP-111 (CAN_SEE), DP-112 (backstab), DP-113 (mindlink), DP-114 (shop hours), DP-115 (steal equipment), DP-116 (consumables), DP-119 (mail/broadcast), DP-120 (junking/brag), DP-121 (slash weapons), DP-122 (ranged combat), DP-123 (equipment weight), DP-124 (money_desc), DP-125/127 (exp tables), DP-128 (affect_spells), DP-131 (clan save), DP-132 (kai_zai), DP-133 (mapcode), DP-134 (weather — documented), DP-135 (spec_procs), DP-136 (infravision), DP-138 (mob wimpy), DP-139 (AFF_ROBBED), DP-140 (improve_skill), DP-141 (non-kill XP)

Rejected: DP-117 (dig — OLC command, not skill), DP-118 (serpent kick — C is also single-target), DP-126 (comm_channel — Go matches C), DP-129 (mail — Go matches C)

### Key findings
- Clan admin destroy was silently losing player data (no SavePlayer call)
- CAN_SEE was awake-only — blind/invisible players could see everything
- Strength system was a no-op — everyone had 50lb carry limit
- Item-use routing was a no-op — potions/scrolls/wands did nothing
- `areGrouped` was bidirectional — C is one-directional (subtle but real bug)
- Weather system: `WeatherChange()` is faithful, `getWeather()` is simplified placeholder

### Subagent patterns that worked
- Pre-digested context (function signatures, exact C lines) = faster completion
- Rotate providers across parallel agents (GLM hit rate limits on 3x same-provider)
- Read C source before creating issues — grep-only scan produced 4 false positives
- Wave 1 (unprepped): 3 agents, 12 min avg. Wave 2 (prepped): 5 agents, 7 min avg.

### What's next
- Full Reek review against cleaned codebase
- Triage Reek findings, create Linear issues, fix
- Lua function implementation (create_event, extra, strlen, foreachi, iscorpse, canget)
- VPS migration when frankendell 2.0 arrives
- Website rebuild from WEBSITE-MASTER-PLAN.md spec

---

## Marathon Session + Fix Session (afternoon/evening)

### Marathon Audit
5 parallel Reek crawls against the full 91,928-line codebase. Each crawl focused on a different domain:
1. Game/Session — concurrency hazards
2. Combat/Spells — spell ID collisions, affect architecture
3. Scripting/Admin — data races, security issues
4. Coverage — under-tested packages
5. Sentinel/Dependencies — stale deps, build health

40 findings total. 15 Linear issues created (DP-142 through DP-156). 5 reports committed.

### Fixes Applied (14 issues resolved)

**Direct fixes (9):**
- DP-145: SpellEnergyDrain 21→25 (verified against C spells.h)
- DP-146: SpellIntellect 81→88 (verified against C globals.lua)
- DP-142: performGiveGold deadlock — pointer-address lock ordering
- DP-148: RateLimitMiddleware removed (dead code, data race)
- DP-149: GetHitroll/GetDamroll — added Equipment.mu.RLock
- DP-151: lookAtChar — tiered condition display (excellent→awful)
- DP-150: TransitItems keyed by instance ID instead of VNum
- DP-152: Status flag affects deduplicated via StackID in NewAffect
- DP-153: AffectManager.Tick() — collect under lock, process outside lock

**Subagent fixes (5):**
- DP-154: Spell damage routes through TakeDamage() (DeepSeek V4 Flash)
- DP-156a: luaCanSee dark room stack fix (Kimi K2.6)
- DP-156b: roomItems map access locked (Kimi K2.6)
- DP-156c: heal/2 in spec_procs4 locked (Kimi K2.6)
- DP-156d: Dreaming AgentID path traversal blocked (Kimi K2.6)

**Rejected (1):**
- DP-147: Ambush sector check — false positive (was correct)

### Commits
- a040592: docs: Reek marathon audit — 5 parallel crawls, 40 findings, 15 Linear issues
- 9819bea: fix: 6 Reek findings — spell collisions, deadlock, data race, health display, dead code
- 971fb3b: fix: 3 Reek findings — transit items, status flag dedup, affect tick lock
- 57fb56b: fix: 5 more Reek findings — subagent batch (DP-154, DP-156)

### Subagent patterns confirmed
- Pre-digested context in task descriptions = faster agent completion
- 2 providers in parallel (DeepSeek + Kimi) — no rate limit conflicts
- Both agents landed clean, build+vet+test passed on first verification
- Kimi K2.6: 4 fixes in 1m11s. DeepSeek V4 Flash: 1 complex fix in 2m26s.

### What's next
- DP-143: Player fields bypass mutex (~267 accesses, ~50 files) — dedicated session
- DP-144: Inventory.Items no lock (92 accesses, ~20 files) — dedicated session, done with DP-143
- DP-155: Dual affect system unification — needs architectural design first
- DP-156 remaining: Admin brute-force, role-by-level, TOCTOU, saving throws
- VPS migration when frankendell 2.0 arrives
- Lua function implementation (create_event, extra, strlen, foreachi, iscorpse, canget)
