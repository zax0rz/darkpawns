# Session Notes — 2026-07-05

## Morning Triage (7:30 AM ET)

**Reek crawl:** 3 findings (DP-939, DP-940, DP-941) — all `cmdTime` fidelity drift
**Results:** 3 confirmed, 0 rejected, 0 needs context

### Findings

- **DP-939 (MED):** `cmdTime` missing weekday display. C computes `((35*month)+day+1)%7` and shows "the Day of the Dark". Go omits it. `WeekdayNames` exists in `constants.go` but is dead code.
- **DP-940 (LOW):** `monthNames` uses Earth names ("January" etc.) instead of C fantasy names ("New Year Tide" etc.). `MonthNames` in `constants.go` has correct C names but is dead code.
- **DP-941 (LOW):** `cmdTime` missing moon phase display when `sunlight==SUN_DARK`. C shows "The moon is [phase]." `Phases` array uses astronomical names instead of C's descriptive names.

### Root Cause

`cmdTime` in `time_weather.go` was written from scratch instead of porting C's `do_time` line-by-line. The correct constants exist in `pkg/game/constants.go` but are dead code.

### Admin Panel

Admin API unreachable (CT 120 down or service not responding). Triages logged to Linear only.

### Grade

Good reek. 3 for 3, all verified against C source.

## Previous Session Context (from Discord)

- 2026-07-05 ~3 AM: Deployed PR #54 (spec proc nil crash fix). Server stable. ~12 mobs with noisy spec procs still panic-ing every tick (caught by recover, logged as errors). Brief written for follow-up.
- PR #50, #51, #52 merged. 18 issues closed. Board clean — 0 CRITICAL, 0 HIGH.
- Server is up and healthy on CT 120.
