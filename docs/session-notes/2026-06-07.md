# Session Notes — 2026-06-07

## Morning Triage (7:30 AM ET)

**Source:** Reek's Sunday Fidelity Deep Dive — admin, optimization, moderation, telnet/ban system

### Findings

| Issue | Severity | Finding | Triage |
|-------|----------|---------|--------|
| DP-553 | CRITICAL | Wildcard ban matching broken — `192.168.1.*` never fires | CONFIRMED |
| DP-554 | HIGH | `ValidName()` missing online player duplicate check | CONFIRMED |
| DP-555 | MEDIUM | Ban file write order uses slice reversal, not recursion | REJECTED |
| DP-556 | MEDIUM | Hardcoded connection limits (200 vs dynamic) | CONFIRMED (low) |

### Key Details

**DP-553 (CRITICAL):** C's `new_descriptor()` constructs wildcard IP variants (`wildhost`, `double_wild`) before checking bans. Go passes only the raw IP. `strings.Contains("192.168.1.5", "192.168.1.*")` is false — `*` is literal. All wildcard bans are dead.

**DP-554 (HIGH):** `HasActiveCharacter` callback is wired up (`manager.go:209`) but never called from `ValidName()`. The comment on `bans.go:259` claims online duplicate checking but the code doesn't do it. Race condition possible between simultaneous `create` commands.

**DP-555 (REJECTED):** Reverse slice iteration produces same output as C's recursive unwind. Mechanism changed, behavior identical.

**DP-556 (CONFIRMED, low):** `maxTotalConns = 200` is hardcoded vs C's dynamic ulimit. Per-IP cap (`maxConnsPerIP = 3`) is an improvement over C.

### Out of Scope
- `moderation/`, `admin/`, `optimization/` packages — all new Go code, no C counterpart. No fidelity concern.

### Reek Grade
3 confirmed, 1 rejected, 4 out of scope. 0% false positive rate on in-scope findings. Good reek.

### Operational Note
- zax0rz confirmed the clawpatch/rerun backlog should be triaged methodically rather than urgently. No active players, so accuracy matters more than speed. Multi-day sweep is acceptable.
