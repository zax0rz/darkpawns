# CMWC golden streams

`seed-42.json` is the oracle-owned golden supplied with the Phase 0a brief.

`seed-1.json` was captured independently by compiling a temporary probe against
the repository's authoritative `src/random.c`. It was not generated from the Go
implementation.
