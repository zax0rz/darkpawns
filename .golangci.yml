version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic

  settings:
    errcheck:
      check-type-assertions: false
      check-blank: false
      exclude-functions:
        - (*os.File).Close
        - (io.Closer).Close
        - (net.Conn).Close
    gocritic:
      enabled-tags:
        - diagnostic
        - performance
      disabled-tags:
        - style
        - experimental
        - opinionated
      disabled-checks:
        - hugeParam

  exclusions:
    rules:
      # Test files get more lenient error handling
      - path: _test\.go
        linters:
          - errcheck
      # staticcheck SA5011 (nil-deref) false-positives on the `if x == nil { t.Fatal() }; x.field`
      # guard idiom in tests — surfaced only under the go1.26.5 toolchain (staticcheck stops tracking
      # that t.Fatal is terminal). Scoped to test files; SA5011 still guards non-test code.
      - path: _test\.go
        linters:
          - staticcheck
        text: "nil pointer dereference|pointer can be nil"
      # Third-party / generated directories
      - path: admin-ui/
        linters:
          - errcheck
          - govet
          - unused
      - path: node_modules/
        linters:
          - errcheck
          - govet
          - unused
      # Existing callers of deprecated types/functions: migration is a separate refactor task
      - text: "GetRoomInWorld is deprecated"
        linters:
          - staticcheck
      - text: "CommandSession is deprecated"
        linters:
          - staticcheck
      - text: "MasterAffect is deprecated"
        linters:
          - staticcheck
      - text: "AffectFromChar is deprecated"
        linters:
          - staticcheck
    paths:
      - admin-ui
      - node_modules
      - website

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
